# Worket 界面统一验收（2026-09-11）

范围：桌面最近活动、历史聊天、进行中 / 已完成 / 归档列表和详情，交接、分割、取消记录，沉淀范围、任务、候选编辑、定义版本、复用、验收，服务连接、改进数据，桌宠状态气泡；后台登录 / 初始化、模型配置、接入、运行记录、改进样本及详情。

## 设计与审查

- 系统无衬线字体；正文和按钮 13px、辅助信息 12px、标题 20px（后台页面标题 28px）。标准控件 32px，圆角 8px；白灰底色、蓝色主操作、红色破坏性操作。
- 工作列表去除重复的“选择沉淀：标题”，保留有可访问名称的复选框。没有选中工作时禁用批量沉淀。
- 详情独立显示并可返回列表。交接 / 完成 / 沉淀为可见操作，其余进入更多菜单；不为八个空状态字段铺设占位块。
- 更多菜单选择后、点击外部或按 Escape 收起。异步工作操作防重复提交并显示失败；后台轮询避免覆盖弹窗和菜单，数据未改变时不重建工作视图。
- 弹窗由自身限制宽高，内层不再声明超过父级的宽度。全部表单控件统一，定义编辑仅在选项类型显示可选值字段。
- 删除页面功能介绍、重复标题与领域实现术语。授权的实际范围及不可恢复后果以短句展示，详细留存和退出规则按需展开；授权状态与服务端行为不改。

## 验收清单

| 页面 / 状态 | 交互检查 | 视觉证据 |
| --- | --- | --- |
| 工作列表 / 详情 / 三种状态 | 选择保留、打开 / 返回、完成 / 继续 / 归档 | 默认 448×760 与最小 360×480，无横向溢出 |
| 最近活动 / 历史聊天 | 首次说明、记录后消失、搜索空结果、关闭 | 来源标题、控件、空状态 |
| 更多 / 交接 / 分割 / 取消记录 | 菜单收起、无执行者、请求失败、确认错误 | 菜单边界、弹窗可达性与焦点 |
| 沉淀 / 候选 / 定义 / 复用 / 验收 | 本地合成服务完整流程、退出偏好重启保留 | 范围、候选、定义、复用、验收截图 |
| 服务 / 改进数据 | 检查连接、高级设置、停止 / 删除样本 | 默认及窄窗口 |
| 桌宠 | 首次说明、完成记录后消失、退出偏好 | 气泡及阴影不裁切 |
| 后台各页 | 合成本地服务登录、保存、切页、样本选择 | 桌面及 390px 窄屏 |

所有合成测试仅证明界面与交互，不代表真实模型、跨执行者接力或生产部署验收。最终运行结果在本轮结束前补充。

## 最终结果

- `npm test`：115 / 115 通过。
- `node scripts/qa-interface.mjs`：33 个合成界面状态，覆盖列表返回、跨标签选择、原生事件入口的渲染响应、生命周期操作、错误反馈、窄窗口菜单、后台初始化 / 登录、配置保存、接入生成、样本评审 / 取消删除 / 删除。桌面 448×760、360×480；后台 1280×900、390×844。
- `node scripts/qa-distillation.mjs --unpackaged --improvement`：隔离 Electron + 本地合成模型服务完整通过；额外验证输入类型切换、问题处理在增删后保留、固定资料在保存后保留、长路径不撑宽弹窗，以及 `panel:shown` 指定工作导航。沉淀、发布定义、复用、验收、停止采集、删除样本及重启偏好持久化通过。10 张截图。
- `node scripts/qa-recording-ui.mjs`：首次上传说明、记录后不重复、退出偏好和窄布局通过。
- `node scripts/qa-electron.mjs`：开发版原生窗口、Dock、再次启动恢复、桌宠形态 / 阴影安全区和主面板尺寸通过。
- 截图人工复核后修正：空定义栏目、编辑器关闭按钮随滚动消失、长固定资料路径撑宽弹窗、后台样本详情按钮布局。最终未发现横向溢出、控件文字裁切或菜单越出最小窗口。

报告与截图保存在本机 `output/interface/`、`output/improvement-desktop/`、`output/playwright/recording/`。截图中的记录与接入凭据均属临时合成环境；未读取真实对话用于本轮 QA。原生文件选择在测试中返回临时资料文件；真实模型、外部执行者交接、自动更新、正式发布包与线上后台不属于本轮运行验收。


## 暖纸色与紧凑布局复核（2026-09-11）

按用户反馈，将默认面板改为 400×660（比 448×760 面积减少约 22%）、标准控件改为 30px、分区 / 卡片间距收紧。白灰蓝色改为暖黄纸色、奶油卡片与棕金操作，静态纸纹无需网络资源。详情移除黄色横幅与重复上传说明，待交接状态并入元信息；首次记录范围及真实错误仍可查看。

`qa-interface.mjs` 的 33 个状态及 `qa-distillation.mjs --unpackaged --improvement` 完整桌面流程再次通过。默认窗口实测宽度 400px、内容宽度 400px；最小 360×480 下菜单仍在窗口内。人工复核列表、详情、等待接手、沉淀确认、候选长表单与后台页面，未发现横向溢出。

打包说明：本地应用已重新构建。完整 `qa:package` 两次被未改动的桌宠阴影安全区断言阻断（底部测得 39.75px，既有阈值为 40px）；没有放宽断言，也未据此修改桌宠。本轮另行对打包面板的 400×660 尺寸、纸色、无横幅及无横向溢出做定向验证。

2026-09-11 文件名链接：资料中的绝对路径改为文件名链接，保留完整路径悬停提示和来源数，移除文件卡片底色与边框。构建、6 项 AppService 测试与 34 个合成界面状态通过；隔离 Electron 验证真实 preload / IPC、系统打开接口调用（shell.openPath 使用测试替身）、中文及特殊字符文件名、文件已移动、系统打开失败、无效条目及非面板来源拒绝。截图：`output/interface/file-link-electron.png`。未打开或上传用户原文件。

2026-09-11 浏览与滚动条：构建及 21 项服务、当前工作、并行记录回归通过。修改原附件后连续三次获取详情不再生成附件版本，主动刷新仍复核并生成新版本。34 个合成界面状态通过；隔离 Electron 确认滚动条计算宽度 6px、滚轮可滚动，文件链接及错误反馈仍正常。截图：`output/interface/scrollbar-electron.png`。没有通过修改系统授权设置来隐藏提示；本轮仅移除浏览阶段的附件访问。

2026-09-11 重启进度修复：HIFU 工作只读核对发现来源事件已记录，但详情状态未跟进。新增 `test/integrations/state-sync.test.ts` 先复现重启后历史消息未提炼，再验证后台轮询、Hook、并发去重、重启游标、提炼失败后重启重试和最近回复投影。全量 116 项测试通过；新增失败重试断言后专项回归通过。隔离 Electron 验证最近回复原文及文件链接、滚动交互，截图 `output/interface/latest-activity-electron.png`。

本机验收补充：现有数据库跳过初始建表，因此处理游标迁移须在已有定义迁移之后独立执行。专项测试先删除该列模拟旧结构，再重启验证回填；包含这项修正的全量 116 项测试通过。

真实记录复核：重启后 HIFU 的来源已处理序号与档案最大序号均为 123，原会话对比无缺失事件。此前未显示的发音与动画要求已进入详情状态，最近回复包含执行者报告的两处修改与 33 秒版本信息。多段回复预览专项 Electron 验证通过。此核对只验证 Worket 记录与显示，不代表验收视频成片。

2026-09-11 v0.1.2 发布前检查：复现旧桌宠底部阴影安全区 39.75px 小于 40px 的发布门槛。将透明窗口高度由 270px 增至 271px，角色与吸附尺寸保持不变；原生桌宠显示及拖动 / 收纳 / 重启验收通过，保留原有 40px 断言。此项解决上文遗留的完整安装包验收阻断。

2026-09-11 最终 ZIP 复核发现 Dock 初始化顺序问题：跨工作区窗口设置后 Dock 曾不可见，原包探针再次调用 `app.dock.show()` 可恢复。将正式 Dock 配置移至窗口初始化及 IPC 注册之后，继续使用原有 Dock 可见性门槛验收。

## 2026-09-18 文档式沉淀审阅

候选改为连续正文，悬停显示条目边界和序号，点击或 Enter 展开单条引用与编辑，章节添加入口收为加号。生成提示词默认简体中文，原始引用及协议字段保留原样。

验证：`npm run build`；核心与服务测试 27 项通过；`node --experimental-strip-types scripts/qa-distillation.mjs --unpackaged --improvement` 通过，覆盖默认收起、hover 序号、键盘展开、编辑后收起正文更新、单条展开、类型切换、固定资料、问题处理、保存、发布、复用及重启。400px 窗口无横向溢出，人工复核 `output/improvement-desktop/02-review-draft.png` 与 `02a-review-edit.png`。这是开发态合成验证，未验证真实模型中文质量，未打包或部署。

## 2026-09-18 后台沉淀与来源误标

旧服务同一快照重放耗时 221 秒、5 次模型调用，复现 INVALID_SOURCE_REF；诊断计数为 0 个未知引用、0 个摘录不匹配、26 个用户来源误标（23 个 tool.call、3 个 agent.response），未输出或额外保留正文。新增回归覆盖误标降级为阻塞确认问题、真实来源类型传递、伪造引用/摘录仍拒绝、活动提醒与已查看状态。桌面验收覆盖关闭面板继续处理、完成不弹窗、桌宠点击进入候选、失败中文提示与后台重试。

验证结果：核心、活动状态和来源工作流 27 项，服务 7 项均通过；开发态与最终打包态的 `qa-distillation.mjs --improvement` 均通过（分别模拟正常提取、错误引用失败、重试恢复，共 6 次模型调用），400px 无横向溢出。已人工复核后台进度和桌宠完成截图。

真实服务验收：新版打包客户端连接 `https://worket.dandi.site` 成功，重试此前失败快照，228 秒完成 4 批提取及汇总，本地状态 AWAITING_REVIEW，33 条正文均含中文；4 个非阻塞提示、0 个阻塞问题。此项证明真实提取、引用校验和本地收取通过，未替代用户内容审阅或实际复用验收。
