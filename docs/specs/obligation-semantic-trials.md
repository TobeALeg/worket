# 第 19 轮：义务撤销、禁止和本次豁免的语义验收

2026-09-23。预注册于首次调用前；输入为新合成对话，不是盲测或真实视频交付。

共同基准：每期必须添加背景音乐，验收引用同一义务，MP4 交付；本期脚本为新输入。

1. 长期取消强制：以后背景音乐可按脚本决定，不加也能验收，不是禁止。新实例不能仍要求必须配乐，也不能误解成禁止；新版本保留 MP4，旧实例保持原义务。
2. 长期禁止：以后所有视频禁止背景音乐，保留口播和必要音效。新实例执行禁止，不能变成可选或仍强制；旧实例不变。
3. 本次豁免：仅本期不要配乐，以后仍必须。通用约定和新实例保持原义务，排除依据可追溯；不能凭此次豁免生成不同内容版本。

走真实模型→Electron 审阅→发布→新旧实例包与验收规则。自动断言只检查行为，不规定必须输出某种关系；保留候选及响应供逐条核对，不能通过替换预期或人工补写候选让模型通过。每例最多 2 次供应商调用，无隐藏重试；错误、未知用量及费用保留。优先观察当前机制，只有实际失败才修改产品。
