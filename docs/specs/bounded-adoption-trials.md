# 整体采纳与局部例外：预注册

2026-09-23，第 23 轮。首次真实调用前固定两个新合成输入和预期。基准：MP4、中文字幕、本期脚本含视觉设定。

一例整体肯定后明确仅采纳 24px 字幕安全区和片尾列来源，金色/1:1 明确仅本期。另一例先采纳四项，再在同一会话撤回视觉两项的长期范围，保留安全区/来源。

两例都应：保留两条原约定且不重复；只增加被明确采纳的安全区、来源规则；金色/1:1 不得进入有效或待审通用规则，保留排除依据；视觉设定仍由已有新脚本输入承载，不凭新取值重定义输入。发布 v2，新实例验收包括新增规则，旧实例仍 v1。范围撤回应按同一会话明确纠正处理，不把它当作不同来源的未决冲突。结果检查在本地，不进入模型输入；文字表述自由，不规定条目 key 或关系类别。

每例最多两次真实调用，共四次；失败保留，不修改原预期制造成功。如需修复，原响应冻结回归并另记新调用预算。用真实 Electron/HTTP/配置模型/审阅/实例和验收列表验证；不执行视频业务，不宣称盲测或实际用户收益。

## 原检查器漏识别等义措辞（追加）

narrowed-after-assent 首次报告 FAILED，因为检查器只识别“安全/边距”，而原响应完整保留“字幕距画面边缘至少 24px”。人工对照原始回应与草稿确认阈值、主体及距离语义均正确，局部撤回也正确。修正检查器增加“距…边缘/边界”表达，不改变原预期、输入或模型输出。预注册 v1 与原 FAILED 报告永久保留；使用同一响应走完整链路，不追加真实调用。
