# 重复、范围提升与模型关系冗余验收

2026-09-23，第 21 轮。预注册与追加配对场景见 [规格](../specs/repetition-semantic-trials.md)。首轮原始预期/脚本/输入/响应/报告及 SHA-256 均冻结在 test/fixtures/repetition-evolution。

首轮 2026-09-22T23-43-04-583Z：同实例重试和 Agent 转述未成为长期双语，通过真实模型→审阅→发布→新实例/实际验收，仍 v1。另两例原始报告为 FAILED，没有删去或重新标成通过：

- repeated-one-off：检查器以英文/双语词出现判为泛化，但实际新增为“长期默认仅提供中文字幕，不改为默认中英双语”。来源含“长期约定仍只有中文字幕”，而基准只写“必须包含中文字幕”。这是不明确的基准与预期，不足以证明产品泛化错误；不为它修改提示或删除有可能有效的限制。冻结回归保留原预期仍失败及该补充的准确内容。
- explicit-promotion：模型语义支持以后默认英文，但把相同的 SUPPLEMENTS/constraints.subtitles 同时写在 change 与 item.rule.relation。严格协议拒绝，无法进入审阅，是真实可复现的实现阻塞。

新增无排他措辞的 repeated-instance-scope 是另一个预注册输入，不是原失败例的重跑。2026-09-22T23-48-07-488Z 的两次实际调用通过：三个独立实例均只保留本期英语要求的排除依据，新实例仍 v1 且只要求原中文字幕。

修复位于后台模型结果入口：仅当冗余关系类型、目标完全相同，且没有其他关系字段时删除重复的 item.rule.relation。不推断自然语言，不补缺失目标，不修正不一致关系；规范领域校验仍拒绝这种冗余输入，范围/来源/目标冲突继续校验。提示版本不变。原始 explicit-promotion 响应保持原样，经新入口回放 23-49-10-664Z 走通 v2 与新实例，实际验收含英文；旧实例仍 v1。不额外收费重新生成正确答案。原输出中的重复时间条件措辞保留，此修复不代表文案或全部语义已优化。

239 项全量测试通过，覆盖四份原始响应、原领域拒绝的复现、无语义修改归一化，以及类型/目标不一致、额外字段、非法目标/来源、INSTANCE 替代等继续拒绝。现有 657c2a6 ZIP 解压客户端两条回放通过：23-51-30-443Z 明确提升为 v2；23-51-35-040Z 多实例仅本期保持 v1。未改写全部首轮套件为通过。

共 8 次真实调用，input 31189 / output 59614 / total 90803 tokens；供应商未返回费用。所有后续回放零新调用。无真实视频或外部 Agent 业务交付，不是盲测或长期收益证据。

本轮客户端运行时无变更，沿用已验证的 657c2a6。后台干净构建与部署待完成；见 [机器结果](repetition-evolution-results.json)。
