# 无变化采集成本验收

2026-09-23，第 17 轮。源码和实际客户端均测量真实采集方法，输入为合成工作，不外发用户历史。

| 负载 | 修改前中位耗时 | 修改后中位耗时 | 30 次检查的完整快照 / 入队尝试 |
| --- | --- | --- | --- |
| 3 项 × 200 条消息 | 17.23 ms | 0.057 ms | 90 / 18,000 → 0 / 0 |
| 3 项 × 2,000 条消息 | 232.97 ms | 0.055 ms | 90 / 180,000 → 0 / 0 |

记录有变化时仍读取完整档案；此表仅为本机 collect 的同步耗时，不能推算整个应用体验、供应商费用或长期业务节省。

真实 Electron + 独立 HTTP 后台源码运行 `2026-09-22T22-43-37-471Z` 通过：含 200 条附加合成来源，40 次无变化同步中采集读取 0 个完整快照、尝试入队 0 次、追加上传 0；后续分页失败原子性、修订、消失与恢复、后台有效原文均正确。上一轮 c08d9eb 解压客户端 `22-45-29-800Z` 同脚本失败：40 个快照、8,160 次入队尝试。

221 项全量测试通过。新增检查覆盖旧格式首次扫描、无变化重启、工具事件不外发、相同/迟到序号不漏、长消息部分排队、检查点事务失败回滚、到期/完成/解除绑定/删除/退出采集。未调用供应商，外部 Agent 边界受控。

入口：`npm run qa:recording-idle`；原始运行目录 output/codex-history；[机器结果](recording-idle-results.json) 保存两版对照。干净试用包待构建验收。
