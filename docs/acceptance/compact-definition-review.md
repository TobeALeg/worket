# 紧凑候选审阅验收

日期：2026-09-21。依据：用户在 HTML 示意中确认的窄浮窗、分类图标、行内决策、只向右调宽及深层全内容编辑。

## 验收范围

- 默认 410px，左侧分类列固定；右边缘调宽保持原生窗口 x/y/height。
- 正文紧凑展示，来源按需展开；编辑全部显示条目、必填、增删和输入设置。
- 按次交付新增条件输入；排除条目与条件交付均可撤销；自定义修改需显式采用。
- 暂存真实数据；重新决定会撤销已存确认；其他条目的编辑不清除无关决定。
- 固定资料绑定不因重绘丢失；长正文滚动而操作栏保持可用；未处理事项阻止发布。

## 证据

`npm test` 编译通过；允许本机测试端口后，完整 Node 测试 139/139 通过（含新增字段定位、暂存后采用修改、撤销确认及窗口边界测试）。

`scripts/qa-distillation.mjs --unpackaged` 通过：使用临时工作库与合成模型，完整执行提取、审阅、编辑、资料选择、暂存、撤销已存确认、保存定义、创建新实例、验收、进程重启恢复，以及失败重试/附件不可用降级。窗口从 410px 增加 20px 时 x 保持不变；最终 scrollWidth = innerWidth = 410。截图位于 `output/distillation/02-review-draft.png`、`02a-review-edit.png`、`02b-review-ready.png`。

最终打包版 `node scripts/qa-distillation.mjs` 同样通过（`unpackaged: false`，12 张截图，完整重启恢复）；最终关联回归 26/26 通过。本地包为 `release/Worket-darwin-arm64/Worket.app`，构建使用本机已缓存且与依赖一致的 Electron 44.1.1。已检查 ASAR 包含新审阅模块、排除了工作区内用户提供的审计 ZIP 与临时审计目录。

这是本地开发及打包验收，不是线上模型质量、真实执行者交付或公开版本发布证据；未推送或发布 GitHub Release。
