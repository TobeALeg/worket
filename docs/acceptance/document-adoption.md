# 规范文件部分采纳验收

2026-09-23。按 [预注册](../specs/document-adoption-trials.md) 新建两例合成 FRAME.md/聊天输入，实际模型只保留采纳的 24px 字幕安全区、片尾来源和 MP4，另保留聊天补充的两行字幕。未采纳 Logo/音乐没有变成义务或禁止；第二例的本次 12px 特批也没有替换长期 24px。文件引用只覆盖已采用的单条原文，不把相邻未采纳条款一起带入。

原运行 00-56-33-863Z 两例报告均 FAILED，原因是检查器要求 materialRoles 完全为空，却把用户提及的既有 skill 依赖当成了重复规范绑定。原响应并没有再次要求绑定 FRAME.md。人工核对两份完整材料角色与提示后，检查器改为禁止额外普通资料绑定、允许独立 SKILL 角色。原始预注册 v1、请求/响应和 FAILED 报告全部按 hash 保留；没有改变模型输出或隐瞒失败。

同一响应在现有 bff67e2 ZIP 解压客户端完成回放：第一例 00-59-44-600Z，第二例 01-00-41-444Z。审阅时仅给模型已提出的技能角色选择一次合成目录；未修改候选要求。删除 FRAME.md 原件后发布的定义仍可新建实例，实际验收列表与认证 MCP 都仅含有效要求，不需要每次重新选择规范或技能。默认窄面板截图已核对，正文滚动及底部操作正常。

254 项全量测试通过。真实调用 4 次，输入 12098、输出 35967、总 48065 tokens，费用未知。回放没有新增模型调用。客户端/提示/后台运行逻辑均不变，无需重新打包或部署；后台维持 8d39223。试验经隔离 HTTP 服务桥接已配置模型，未向生产创建业务数据；解压用 mock keychain，不代表系统钥匙串验收。

本轮增加语义及端到端回归，修复的是评测器分类错误，不宣称新增模型能力、普遍准确率或用户实际节时。没有外部 Agent 或视频业务交付。见 [机器结果](document-adoption-results.json)。
