# 整体采纳与局部例外验收

2026-09-23。按 [预注册](../specs/bounded-adoption-trials.md) 固定两例新合成来源：整体同意但局部例外，以及先全部采纳再撤回配色/尺寸的长期范围。真实模型在两例都保留 24px 字幕安全区、片尾来源，并把金色/1:1 留作本次排除依据。原中文字幕和 MP4 保留一次，旧实例保持 v1，新实例 v2 不携带旧视觉设定。

真实运行 00-20-48-046Z 第一例完整通过；第二例原始报告 FAILED。失败来自检查器不识别“距画面边缘至少 24px”，而非模型遗漏。原始预注册代码、输入、响应和 FAILED 报告均按 hash 冻结；仅补充检查器的等义表达，不改变输入或预期，也不修改模型响应。修正后同一原响应在 00-25-57-685Z 两条解压端到端回放均通过，覆盖审阅、发布、实例及实际验收列表。

247 项全量测试通过。共 4 次真实模型调用，输入 15418、输出 46518、总 61936 tokens，费用未知；回放零调用。客户端为已签名的 325e7ec ZIP 解压版本，后台逻辑为 8d39223，隔离 HTTP 服务仅桥接现有配置的模型，不向生产创建业务记录。无需改提示、运行代码或重新部署。

本轮增加可复现的语义边界覆盖，不能据此宣称新增产品能力或普遍准确率。未执行真实视频业务，未测用户节省时间；解压测试使用 mock keychain。见 [机器结果](bounded-adoption-results.json)。
