# 组合技能相对路径验收

2026-09-23。本机 HyperFrames 入口存在跨技能引用（如 ../media-use），此前只读分析未执行该技能。随后预注册两个合成技能：report-builder 跨目录读取 format-kit 的资料和模块。旧 84c5480 解压包 00-39-33-365Z 中，两份技能暂存、重启和发布成功，但新实例里的 ../format-kit 路径 ENOENT，证明逐个固定目录不足以保持执行关系。

现在工作包为同一不可变定义的多个已选技能生成共同目录，保留原目录名；原始固定副本和定义内容保持。明确同名冲突拒绝，缓存重建、删除清理沿用固定版本的所有权。源码完整 00-47-33-385Z 通过：同名冲突不部分保存、重开/重启、删除原件、发布、新实例、跨目录模块执行、认证 MCP、派生目录损坏后 MCP 重建，以及仅更新依赖后 v2 新实例与 v1 旧实例隔离。较早 00-41-13-130Z 基础链路也通过。

252 项全量测试通过，包含未选邻居技能不收集、同名及大小写冲突、空目录和权限位、派生损坏/额外文件重建、原始固定副本缺失拒绝、符号链接边界、删除一个定义保留另一份定义及共享技能。单技能暂存/失败撤销/重启/发布/MCP 回归 00-43-04-881Z 通过。410×700 审阅截图已查看，两个已固定技能和底部操作可见。

零真实模型调用。执行的是本轮自建技能脚本；未执行真实 HyperFrames 视频、插件安装或外部 Agent 业务验收，不把文件依赖齐全等同于运行环境完整。后台协议和运行代码不变，待干净构建客户端并解压验收。见 [机器结果](combined-skills-results.json)。
