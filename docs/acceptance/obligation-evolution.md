# 义务撤销、禁止和本次豁免验收

2026-09-23，第 19 轮。预注册标准见 [语义场景](../specs/obligation-semantic-trials.md)，不是用户实际视频交付或盲测。

第一轮真实调用 `2026-09-22T23-08-29-087Z`：长期取消强制的语义正确，原检查器把“这不是禁止配乐”中的“禁止”误判为禁止，未继续发布；本次豁免完整通过且不产生新版本；长期禁止实际在 RULE_SCOPE_MISMATCH 失败。原始响应、错误与报告完整冻结于 test/fixtures/obligation-evolution，没有人工修改候选来通过。

真实失败原因：模型把“所有视频，包括开头和结尾，无论脚本怎么写”写进 condition，而基准是无条件义务。修复演进提示，明确通用覆盖范围不属于限定条件；适用范围不变的替代/重复沿用基准 condition 原值，无条件时不新增 condition。真正条件改变仍需要保留旧规则并审阅，程序范围校验不放宽。新技能协议路径提示版本 v1.7，旧能力路径分别 v1.5.1/v1.4.1。

`2026-09-22T23-12-27-707Z` 长期禁止真实复验通过：新版执行禁止，旧实例仍使用旧版。`23-15-29-426Z` 额外真实平台条件回归通过：Instagram/TikTok 不同上限保留，本期特批排除，Instagram 的新增话题要求仍限于该平台。

修正检查器后，撤销原响应在 `23-12-37-289Z` 完整回放通过；没有为测试误报重复收费调用。三例源码回放 `23-15-36-882Z` 与 db8dfa2 ZIP 解压客户端回放 `23-17-42-010Z` 全部通过，实际验收列表分别显示可选、禁止、原有必须；取消强制和禁止生成 v2，本次豁免仍为 v1，旧实例均保持 v1。导出的规则已求有效集合，检查器不再对历史关系做第二次图解析。回放只能证明已记录答案走通当前产品链路，不能代表新提示下所有输出均正确。

230 项全量测试通过，包含新真实成功响应和旧错误响应仍被拒绝。共 10 次真实供应商调用，input 33,771 / output 51,794 / total 85,565 tokens；供应商未返回费用，未知，不记为零。端到端通过不等于真实视频验收或长期节时。

本轮桌面运行时代码未变，沿用 db8dfa2 试用包，已验证其兼容新后台流程；无需重复构建客户端。后台 b4702a0 已从提交干净构建部署，公网健康/能力检查通过，evolution-prompt.mjs、workflow.mjs、contracts/evolution.js 三份运行 hash 与干净构建一致；原容器配置保持。备份与回滚见 [运维记录](../deployment/jp-server-worket.md)。详细记录见 [机器结果](obligation-evolution-results.json)。
