# 沉淀输入过滤验证

日期：2026-09-24。状态：源码实现、完整回归、真实模型与客户端隔离验收通过。未部署生产，未替换已安装客户端。

## 原问题与实际范围

任务 `39a60cbd-9323-4d18-8547-54b21739dcb4`，原快照 `852dabb1-6909-4838-8da9-92e84e3ea3b7`，旧状态 INTERRUPTED，错误为 `INPUT_TOO_LARGE`。当前旧流程回放通过 2 MiB 总量检查，但单条记录超过 24,000 字节。仅拆分还会触及批次数及全量原文汇总上限。

离线重放只读本机工作库并创建隔离副本，通过真实 `DistillationService.retry → wire` 构造新任务；原数据库、旧任务和旧快照保持不变。脚本：

```sh
node scripts/qa-distillation-input.mjs prepare OUTPUT_DIR SOURCE_DB JOB_ID
```

`OUTPUT_DIR` 必须是新的目录。导出的 request.json 为实际发送内容，preflight.json 仅包含指标；私有数据库与请求留在被 Git 忽略的 output 目录，未提交。

## 本轮快照结果

| 检查 | 结果 |
| --- | --- |
| 原始事件 | 599 |
| 原请求 | 750,278 字节 |
| 实际精简请求 | 455,631 字节，减少约 39.3% |
| 实现代码读取正文省略 | 36 条 |
| 实现代码写入正文省略 | 45 条 |
| 模型实际输入事件 | 518 |
| 用户消息 / Agent 回复 / 标题 | 7 / 85 / 1，逐字保留 |
| 分块预算 | 19 批提取，加 1 次汇总，符合 20 次调用限额 |
| 原快照 | hash 校验未改变 |
| 真实模型调用 | 共 21 次：19 批提取 + 两轮各 1 次真实汇总 |
| 最终服务 / 客户端 | SUCCEEDED / AWAITING_REVIEW |
| 最终候选 | 48 条；32 项阻止发布的待确认问题，另有 6 项提示 |

保守规则保留了 Markdown 规范、未知或混合 shell 输出、失败记录和一条无法完整解析的嵌套档案。没有为了达到此前 33.6 KB 的极端排除对照而删掉全部工具内容。不同调用的相同输出仍保留，避免丢失后一次执行的状态。

## 回归范围

`npm run build` 与 `npm run build:server` 通过；最终完整回归 `node --experimental-strip-types --test test/**/*.test.ts` 为 284/284 通过。首轮受沙箱禁止本机监听影响的 HTTP 测试已在获批的本机环境重跑通过。

`test/distillation/analysis-input.test.ts` 覆盖用户代码块与直接/含混引用、规范读取与写入、失败诊断、未知工具、literal cat/sed 与混合命令区别、成功 TAP 结果原文、同一调用重复与不同调用时间线、生成脚本、同工作档案副本核验、中英文/emoji 无损分片、分片引用与汇总证据、旧快照重试隔离、后台能力门控、密集引用预算、规范文件身份，以及未确认跨范围重复关系与 ACTIVE 冲突仍拒绝。

`test/distillation/server.test.ts` 通过认证 HTTP 路径验证新能力、实际输入覆盖统计和不回显原文的超限说明。既有旧协议的来源身份、引用、规则、权限、取消和来源删除测试继续运行。结构测试和离线预算通过不等于真实模型能够完成，亦不证明候选语义质量。

## 真实验证边界

已把该固定精简请求提交到现有 `jp-server` 上的隔离服务，使用当前配置的 `deepseek-flash`。隔离服务使用临时元数据数据库，不修改生产容器、服务配置或用户定义；凭据只在服务器内读取。生成结果通过本地 `DistillationService.receive` 导入隔离数据库，实测进入 AWAITING_REVIEW，没有发布定义。

首次上传曾被自动审批审核拒绝。用户随后明确“以后都可以直接测真实模型”，已据此开始在现有服务与模型配置范围内重测。隔离环境只读取模型配置与解密材料，生成独立测试身份，不读取生产身份签名密钥。

## 真实长记录发现的汇总预算问题

首次真实调用由 `deepseek-flash` 完成全部 19 批提取，耗时 335.337 秒；每批覆盖与引用均通过，但 191 条候选及 189 段原文汇总为 105,319 字节，触及 96,000 字节限制，因此首次任务仍为 FAILED / INPUT_TOO_LARGE，没有汇总模型调用。

修复移除普通事件片段重复携带的原始 hash；hash 仍在固定请求与本地档案中参加验证，规范文件引用继续保留最终条款必需的文件 hash。汇总变为 91,333 字节，离线逐项比较确认全部候选、关系和引用正文不变。新的密集引用回归验证保留每条引用与候选关系时可通过预算。

继续验证使用首次调用的固定代码档案重新生成 19 份模型输入，对每份输入消息和真实输出文件生成 SHA-256 校验清单。补跑时任何输入、模型身份或结果 hash 变化均拒绝复用；重新通过每批覆盖和引用检查后，只新增一次真实汇总调用。它不是重新调用完整 20 次的新采样，须与首轮真实提取一起报告。

## 真实结果的跨范围重复关系

第二轮仅新增 1 次真实汇总，耗时 185.310 秒，原始结果触发 RULE_SCOPE_MISMATCH：模型把 UNCERTAIN / PROPOSED 的“新候选中文、旧候选不翻译”验收建议，标成 REUSABLE / ACTIVE 的“默认中文”要求的 DUPLICATE。

修复只处理这一保守边界：未确认、非通用候选不能被合并进正式通用规则；移除错误 DUPLICATE 关系，保留两条完整内容、来源及状态，并添加阻止发布的待确认问题。不会把建议提升为已采纳，也不更改正式规则；ACTIVE 跨范围合并和 REPLACES 仍由原验证器拒绝。固定真实输出回放已通过，其后再使用同一 19 批原始提取结果，新增真实汇总调用验证修复后的服务。

## 最终真实验收

第三轮由修复后的 `a5b53b0` 代码运行，复用校验过的 19 批真实提取结果，再新增 1 次真实汇总；`deepseek-flash` 在 140.286 秒后返回通过全部验证的结果，服务状态 SUCCEEDED。此次新生成结果没有跨范围 DUPLICATE 错误；该边界修复另由第二轮真实输出回放及自动回归验证。

本机通过 `node scripts/qa-distillation-input.mjs receive output/distillation-analysis/final` 使用真实 `DistillationService.receive` 接收，隔离任务进入 AWAITING_REVIEW。生成 48 条候选，覆盖实际输入的 518 条事件、19 个分块；32 项需人工确认后才能发布，另有 6 项提示。来源数、引用原文、文件身份和规则结构校验均保留。

人工抽查确认原用户的默认中文、连续正文、悬停辨别条目、点击引用与编辑、快速审阅提交、后台处理动效及桌宠完成反馈均出现在候选中，并引用原用户消息。该抽查不等于全部候选的语义准确率评测；Agent 建议、推断及临时要求仍需要用户审阅。

完整调试共 21 次真实调用，供应商报告 prompt_tokens=234,574、completion_tokens=269,309、total_tokens=503,883（包含前两轮失败成本，不推算金额）。成功路径沿用的首次提取耗时 335.337 秒，最后汇总耗时 140.286 秒；不能把有缓存的末轮耗时当成从零处理时间，也未声称对原文完整重新采样 20 次。

原数据库中的失败任务和原快照逐项与隔离副本内的旧版本比较，仍完全一致。新任务、草稿与完整模型输出只存于忽略的 `output/distillation-analysis/final/`；关键文件为 `accepted.json`、`live-summary.json`、`attempt-1/2/3/` 和供人工阅读的 `review.md`。固定代码档案、请求与复用响应均有校验记录；不提交工作正文或模型凭据。

验收后已删除服务器上本次创建的临时代码与记录目录，隔离容器已退出。生产仍运行 `/opt/worket/releases/8ffa545`、原镜像与 uid 1000，公网 `/health` 返回正常。

## 复用 HTML 人工确认页

2026-09-24 用户要求找回已有 HTML 确认页。找到 `codex/conversation-requirements-research` 分支中的 `experiments/conversation-requirements/review/`（原提交 `8d675b2`、`c3994eb`），复用其页面及持久化流程，移入 `scripts/lib/human-review/` 并接入本次实际成功结果。旧研究工作区和审阅结果保持。

启动命令：`node scripts/review-distillation.mjs output/distillation-analysis/final 4319`（新检出需先 `npm run build`）。本机入口为 `http://127.0.0.1:4319/#distillation-4d7816c0-7edf-4e06-810a-c64bb1d413f0`。48 条候选和 6 个独立问题共 54 个审阅步骤，另有 32 个问题按原字段关联在候选旁；与“32 项阻断、6 项非阻断”的分类维度不同。全部 38 个问题均保留，原始 599 条事件可查看。

沿用接受、修改、拒绝、暂放、撤销、逐字引用、编辑暂存、遗漏补充、提交和导出。正式进度保存于 `output/distillation-analysis/final/human-reviews/`；这里只记录用户对提炼结果的判断，尚未接入定义发布。交付前正式页面为 0/54，目录无审阅 JSON，没有替用户作决定。新增模型调用为零。

`npm test` 完成构建及 294/294 测试。新增覆盖结果投影、来源身份和摘录验证、未确认范围与建议状态、问题完整保留，以及旧审阅的幂等、冲突、撤销、提交门槛、落盘重启和 HTTP 同源保护。浏览器在 `html-qa-20260924/` 独立目录验证接受自动前进、撤销、编辑暂存刷新恢复、保存修改、原文前后文、关联问题显示及未完成时提交禁用；页面错误日志为空。正式页面另行核对初始状态和实际渲染后打开给用户，用户本人尚未完成审阅。
