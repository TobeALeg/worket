# 条件、冲突与部分采纳验收

日期：2026-09-23。三类新合成输入，期望事先写入本地验收函数；模型只接收已确认基准与源交互，不接收 Gold/期望结果。每次案例最多两次模型调用，不做隐式重试。最终并非盲测，也不代表真实视频成片或节时收益。

## 首轮与失败

`2026-09-22T18-47-54-959Z`：条件与例外通过（Instagram 50/TikTok 80 各保留一次，新增 Instagram #Acme，本期 70 的特批不通用化）；跨实例冲突通过（同一条件下 30 与 70 的长期修改阻断发布）。部分采纳在协调阶段服务失败，没有原始最终返回或 usage，不能判断语义结果。首轮六次调用尝试，五次有 usage。

仅第三例重做，`18-52-57-814Z`：语义输出正确区分字幕采纳、Logo/音乐未决定，但把没有可复用要求的脚本值列进 exclusions，被严格覆盖校验拒绝。原始输出完整保留为 INCOMPLETE_COVERAGE 失败，未修改答案或放宽校验。

增加“无新增规则仍为已处理事件”的明确提示后，`18-56-26-498Z`：覆盖通过，字幕与未采纳项也正确分开；但字幕同时引用 Agent 原建议和用户的具体采纳语句，被旧来源审计降为推断并增加一次审阅。保留为 UNSUPPORTED_SOURCE 已知案例。

开发过程另有两项验收初始化问题：最早手工基准漏填 USER_AUTHORED.reviewEventId，在模型调用前被拒绝；`19-02-08-307Z` 构建类型错误使静态页面未拷贝，桌面未启动、模型调用为零。类型检查已修复，不将这些失败归因于模型。

## 修复与最终证据

实现 [直接依据与背景引用](../specs/evidence-roles.md)，保留具体用户采纳锚点，让原建议仅作为背景；没有移除审阅确认。新增输入值不再要求模型重复确认既有输入角色。

`2026-09-22T19-04-49-118Z`：新版 v1.2 在第三例通过真实模型 → Electron → HTTP → 依据与提议审阅 → 新版约定 → 新输入实例，旧实例仍为 v1。字幕生效，Logo/音乐不进入有效规则。

原始请求、响应、用量和 SHA-256 固定在 `test/fixtures/semantic-evolution`，包括服务失败、覆盖失败与混合归因旧结果。严格回放比对输入及中间结果，不把不同输入配旧答案。

最终源码桌面回放 `2026-09-22T19-09-05-534Z` 三例通过，零新增模型调用。冲突例先证明发布被阻断，再由合成用户明确选择 30 words、在实际界面编辑确认、新实例执行 30；不把这项人工裁决记为模型自动判断。首次扩展回放 `19-07-52-446Z` 的冲突路径因测试忘记退出编辑模式失败，补齐操作后通过，产品未因此放宽发布条件。部分采纳例展开直接依据/背景引用后再确认。

去重同一未采纳建议的具体问题与兜底提示后，最终三例桌面回放 `2026-09-22T19-14-18-849Z` 通过；两个提议只显示两项决定，其他问题类型仍独立保留。全量 178 项测试通过，包含已知失败的严格拒绝、旧请求兼容、无直接依据/伪造摘录拒绝、来源角色本机映射与持久化。旧三类和主动/持续链路回放结果另见持续优化机器记录。

共 12 次真实模型调用尝试，其中 11 次已返回用量：input 30,715 / output 93,957 / total 124,672 tokens。另一次失败用量未知；供应商没有返回费用，不记为零。详细每次结果见 [机器记录](contract-semantic-trials-results.json)。

源码提交 `46b2c1a`。干净导出构建的 ZIP 解压客户端三例通过 `2026-09-22T19-24-58-636Z`；原包与解压签名验证通过，隔离测试钥匙串，正常系统钥匙串批准未验证。试用包 `release/Worket-loop-46b2c1a.zip`，SHA-256 `610d1f31455429726dbe6ffe399499f90c988c8cff3ad98e9c41a2ed1500f755`。同版后台已部署，公网认证能力返回 rule/evolution/evidence schema 1；无新增模型调用。

## 下一步已复现问题

执行路径预检查发现 `get_work_context` 直接返回旧交接包：交接后新增资料，MCP 返回 neededArtifacts 仍为 0，而当前工作已为 1。隔离复现证据 [已保存复现](mcp-context-freshness-repro.json)。后续第 7 轮已修复并通过源码与解压客户端 MCP 验收，见 [当前工作包](mcp-context-freshness.md)；该证据仍不等于目标 Agent 完成业务交付。
