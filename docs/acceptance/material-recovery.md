# 固定资料恢复验收

2026-09-23，第 18 轮。

旧 7dd6c4a 解压客户端 `2026-09-22T22-53-50-983Z` 在真实“本次文件”恢复步骤失败；已目视检查截图，显示“固定资料副本缺失或校验不符”。同样操作源码 `22-56-57-461Z` 通过，随后继续完成两次实例、原文件删除、输入更新、MCP 回执和 legacy 修复。

完整恢复入口源码 `22-58-44-392Z` 通过：定义页拒绝不同文件/技能版本，原版恢复缺失文件、损坏技能及多余文件；工作页恢复输入及参考；contract ID/hash/版本数量和实例清单保持，HTTP MCP 校验通过。输入为显式合成资料，选择器提供隔离文件；外部 Agent 未执行，供应商调用为零。

225 项全量回归通过。新增边界覆盖文件缺失/不可读、错误版本不写入、具名输入重选、技能全部清单/权限恢复、外部路径/符号链接拒绝及目录切换异常回滚。断电不是普通异常，不把双 rename 说成断电原子操作；中途断电仍可能留下备份并需要再次显式恢复。

入口 `npm run qa:material-recovery`。原始运行在 output/material-recovery 与 output/instance-files，[机器结果](material-recovery-results.json) 保留失败及通过。干净打包验收待完成。
