# 来源变化后的候选审阅验收

2026-09-23。旧 db8dfa2 ZIP 解压客户端在 `2026-09-22T23-28-32-154Z` 复现：记录及新提炼范围已使用修订原文，已有候选审阅没有来源变化提示。更早 `23-27-55-415Z` 是合成检查器引用非用户事件造成的 INVALID_SOURCE_REF，已修正引用选择，不记为产品缺陷；原失败报告保留。

新增聊天来源对照和明确采用当前候选的确认，发布事务检查变化 hash；原快照不变。审阅时只核对本地已同步记录，新增未选消息、已确认版本的历史依据不会增加确认。文件固定版本不随此机制变化。

233 项全量测试通过；第一次执行的本地 HTTP 监听被沙箱 EPERM 拒绝，取得执行权限后全量通过。源码 Electron 来源修订→原文对照→打开审阅时后台二次修订→旧确认失效→再次确认→发布→新输入实例，在 `23-33-34-952Z` 通过；默认 410×700 面板 `23-34-20-975Z` 通过，截图已查看，正文滚动、底部按钮可见。`23-33-05-495Z` 仅为检查器文字定位歧义，已改精确匹配。

`23-35-34-337Z` 真实 stdio 子进程→Codex 完整读取→缺失检测→Electron 审阅链路通过：已缺失原文保留并标示，未确认时不能发布，恢复后排除缺失标记并可明确发布。冻结的三种义务演进回放 `23-34-27-081Z` 全部通过，无变化来源没有新增确认步骤。

本轮零真实供应商调用。候选由受控合成模型边界经真实 DistillationService 验证产生，来源同步和桌面/MCP 为真实产品路径；没有外部 Agent 业务执行或用户实际交付验收。检查范围受本地同步新鲜度限制，不能发现尚未同步的外部修改。

客户端干净构建和 ZIP 解压验收待完成；后台保持 b4702a0，不需要服务协议或后台运行代码更新。详细运行报告见 [机器结果](source-review-results.json)。
