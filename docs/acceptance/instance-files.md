# 实例文件隔离验收

2026-09-23，第 16 轮。

复现：同一 SCRIPT.md 被两次创建选入，覆盖为第二期后，旧客户端第一次工作的包读到“第二期脚本”。直接 core 复现与上一版 44dd3ba 解压客户端的同一端到端脚本均失败；完整失败保留于机器结果。

修复：本次输入/参考固定到各实例；所有包出口验证副本。旧实例缺少固定证据须明确重选，不能静默采用今天的原文件。“本次文件”更改仅影响当前实例，保留历史副本/来源，重置读取回执并要求 MCP 再读。

217 项完整测试通过。源码 Electron `2026-09-22T22-32-38-849Z` 通过：分别从 v1、v2 创建工作；本次特殊要求不继承；覆盖/删除原 SCRIPT.md 不影响已固定输入；换文件后等待接手，实际 HTTP MCP 读到新文件才确认接手；其他实例和旧副本不变；模拟旧格式实例导出受阻后，通过真实文件选择入口恢复。旧包 `2026-09-22T22-33-49-879Z` 在第一次工作串读第二期脚本处失败。

这是合成输入的真实 Electron、SQLite、HTTP MCP 路径；外部 Agent 启动和历史边界受控，文件选择由隔离路径提供。新增供应商调用 0，未声称真实业务验收、跨机器文件传输或正常钥匙串授权。

入口 `npm run qa:instance-files`；原始运行目录在 output/instance-files，摘要及失败见 [机器结果](instance-files-results.json)。干净构建及 ZIP 解压验收待完成。
