# 草稿资料暂存验收

2026-09-23。旧 657c2a6 ZIP 解压客户端在 00-00-49-963Z 复现：选择必需文件和技能，点击“暂存”，关闭后重开，两个选择均丢失。保存内容已存在，但资料选择只留在 renderer 内存。

新增草稿固定副本及所有权清理；修订入口恢复同一基准版本的未发布手动草稿。245 项全量测试通过，覆盖重启、原件删除、替换、角色类型变化、事务回滚、共享所有权、取消和永久删除。

源码 Electron 00-11-37-961Z 通过：选择→暂存→关闭重开→重启→修改并删除原件→发布→新输入实例→认证 MCP 读取→受控技能脚本执行；手动修订暂存重开→发布 v2，旧实例仍读 v1。410×700 默认窄面板截图已查看，两个“已固定”状态和底部操作可见。既有资料修复回归 00-11-40-338Z 通过。

保留失败：00-07-49-436Z 是验收环境未关闭可选上传、没有后台配置；00-09-24-009Z 是检查器误用 materials 而非 fixedMaterials；00-10-37-287Z 则揭示第二个真实产品问题——手动修订每次新建草稿导致无法恢复。这些报告保留在 output/draft-materials。一次自动审批超时后按工具指示重试成功，无剩余权限阻塞。

本轮真实供应商调用为零。候选为受控 FixtureClient，经真实服务校验；桌面、数据库、文件及 MCP 是产品路径。执行的是本轮自建合成技能，不是外部 Agent 的真实业务验收。未验证跨机器传输、系统钥匙串授权或断电恢复。

安装包解压验收待当前源码提交后执行；后台保持 8d39223，无需部署。
