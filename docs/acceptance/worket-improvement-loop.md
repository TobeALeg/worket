# Worket 持续优化检查点

2026-09-23 用户批准执行。起点 `0421c9b`，按 [已确认顺序](../specs/worket-improvement-loop.md) 推进。

## 第 1 轮：冻结端到端基线（已验收）

问题：旧验收依赖被忽略的本机 output 目录；回放没有核对输入，可能把历史答案用于不同输入而误报通过。

改动：冻结三份成功与两份失败的原始真实模型调用，包含输入、响应、usage、来源运行号与 SHA-256。它们都是合成业务输入，参与过调参，不属于盲测。原失败分别是错误文件绑定结构和补充项重复数字限制；保留原输出，不把可复现失败算成质量通过。

入口：`npm test` 包含五份 workflow 回放及错配阻断；`npm run qa:contract` 走三类真实 Electron/HTTP/审阅/发布/新实例/复制/文件版本/验收路径。回放不调用供应商，提示词变化不意味着新模型已经通过。

验证：155/155 测试通过（含 6 项新回放检查），评测工具 34/34 通过。三类 Electron 全路径分别通过：video `2026-09-22T17-13-41-979Z`、report `17-13-49-564Z`、code `17-13-54-232Z`，均零供应商调用。

提交：`9b001d9`。

## 第 2 轮：修改规则后的依赖再审（已验收）

发现：手工修改规则会保留旧合并/验收关系的确认，文件更新仅检查直接依赖。现在由 DefinitionRepository.update 统一计算受影响关系，向下追踪间接依赖；将关联项标为待确认，撤销过期决定，重复修改复用同一待定项。保持严格的无循环、范围与有效目标校验。

审阅页在保存后刷新待定项映射；点击确认保存若产生新问题，留在草稿，不能直接发布。历史定义和实例不变，无关规则不增加确认。

验证：156/156 测试通过。Electron 三类回放通过：video `2026-09-22T17-18-58-864Z`、report `17-19-05-091Z`、code `17-19-10-192Z`。报告场景新增真实界面操作：手改上游条款 → 点击确认保存 → 关联验收项出现 → 新版本尚未发布。零新增供应商调用。

提交：`969dcd0`。

## 第 3 轮：替代重复组与证据归属（已验收）

发现：REPLACES 指向重复别名时，旧主条目可能仍然有效；旧重复条目的证据会被转到不同含义的新要求。

改动：先沿 DUPLICATE 找同组保留项，再解析 REPLACES。替代组内任一别名均替代整组；多个替代指向不同别名也会判为冲突。仅同义合并传播依据，替代链不把旧证据当新要求的证据。历史候选与来源仍完整保留，验收引用跟随有效新规则。

验证：158/158 测试通过，包括发布→新实例→交接包→验收引用路径。三类 Electron 回放通过：video `2026-09-22T17-22-14-380Z`、report `17-22-21-104Z`、code `17-22-25-997Z`。没有新增供应商调用或业务交付宣称。

提交：`858c677`。

下一步：推进新交互与既有 contract 的增量协调。增量实现须确定性保留旧规则、固定资料与输入角色；模型只能建议变更，不能靠漏输出删除旧规则。新版本仍需审阅确认。

## 第 4 轮：主动增量比较（已打包部署验收）

实现“从后续工作更新”：基准有效约定加新交互，经模型输出明确差异，由程序保留未变规则、输入和资料。重复只追加基于原版本的依据，不创建相同内容的新版本；本次/撤销要求不进入通用约定。差异审阅可展开完整约定及重复来源，定义页可查看后续原文。确认后按事件 ID/hash 记录比较进度，重启恢复；准备、提交和发布均防止过期基准覆盖新版本。资料角色改变须重新绑定。

真实模型运行 `2026-09-22T17-50-59-469Z` 首次通过：两轮新交互、4 次供应商调用，总 input 11,465 / output 31,084 / total 42,549 tokens；费用未由供应商返回，不推算为零。第一轮 30→20 words、新增双语字幕、MP4 重复及 12 秒例外；第二轮只重复已有要求，版本不增加。新旧实例和固定文件隔离通过。此为合成输入的真实模型链路，不是用户实际成片或盲测。

最终源码全量 162 项测试通过；随后增加并通过资料重新绑定场景（增量测试共 5 项）。评测工具 34/34 通过。旧三类桌面回放 `18-04-25-389Z`、`18-04-29-591Z`、`18-04-34-388Z` 均通过；最终增量回放 `2026-09-22T18-06-11-134Z` 通过，含真实界面依据展开。原始响应与 SHA-256 固定在 test/fixtures/evolution，后续回放零供应商调用。

开发中最初的测试缺少来源绑定，被 WORK_NOT_CAPTURING 正确拒绝，已修复测试初始化；另发现能力协商前拒绝被标为“发送结果不确定”，现依据是否实际尝试提交区分 FAILED/INTERRUPTED。未删除失败样本或用人工补写要求让模型通过。

提交 `5e1800e`。试用包 `release/Worket-loop-5e1800e.zip`，SHA-256 `35448197dcd08185f4d60356a15a07bc75788b46f423b79b0cf66d546edf2a29`。从该提交干净导出构建，原包与解压包签名通过。最终解压客户端的三类旧流程和增量流程分别通过 `2026-09-22T18-10-36-775Z`、`18-10-46-599Z`、`18-10-51-175Z`、`18-10-55-495Z`。仍使用隔离测试钥匙串，未宣称验证正常 OS 钥匙串批准。

后台已更新到同一源码，public health 200，认证 capabilities 200 且支持 rule/evolution schema 1，未认证 401，admin 404。无数据库迁移或密钥配置变更，回滚与备份见部署记录。

详情见 [增量比较](../specs/contract-incremental-evolution.md)。本轮仅为主动选择入口，不等于已实现自动持续学习。下一轮：明确选择范围的持续触发与低打扰待审，并补复杂条件和跨工作冲突验收。

## 第 5 轮：明确授权后的持续触发（已打包验收）

新增每份约定的持续比较开关，默认关闭，只发送开启后该系列实例新记录的用户消息、Agent 回复和本次输入。不会自动读新附件正文或开启其他会话记录，也不加入参与改进样本。两分钟合批、待审阻塞、同批不自动重试、每系列滚动 24 小时最多三次尝试；开关和重启不清空限额。后台协议和模型提示不变，结果沿用现有差异审阅、确认游标与版本隔离。

目的地授权在开启、能力查询后以及连接恢复后的真实发送前检查；关闭、切换服务、删除来源或更新基准时不会误发。自动任务及其显式重试保留原服务身份，状态查询、取消和接收确认也不会转发到新服务。初次全量回归发现 GET 漏接发送守卫，新增测试准确暴露后已修复；不隐去该开发失败。

全量 170 项测试通过，随后新增的显式重试目的地场景在持续比较 8 项测试中通过（当前共 171 项）。首次完整桌面运行 `2026-09-22T18-26-34-129Z` 通过：明确授权、未来范围、产品计时器、HTTP 提交、待审不积压、确认新版本、停止后不再发起。QA 仅提前隔离数据库的稳定观察时间，产品无快进 API。初次启动和全量测试的沙箱监听限制在授权执行后解除。

持续阶段为受控协议响应，旧部分为冻结真实模型输出回放，新增供应商调用为零。此轮验证触发机制与权限边界，不是新语义质量或真实业务交付证据。最终源码桌面回归三类旧流程、主动增量与持续触发均通过，持续触发最终运行号 `2026-09-22T18-32-19-823Z`，全部运行号见机器结果；源码提交 `9a27f45`。干净导出构建的试用包 `release/Worket-loop-9a27f45.zip`，SHA-256 `42300b896d9f07ba0fe4dc2cc4e0c2eeedc469fe7086aa1206620e261b5f6fd3`。原包及 ZIP 解压签名验证通过；解压客户端的主动增量与持续比较分别通过 `2026-09-22T18-41-34-374Z`、`2026-09-22T18-41-47-995Z`。打包 QA 使用隔离测试钥匙串，正常系统钥匙串批准仍未验证；未替换已安装客户端或发布 GitHub Release。后台继续使用已验收的 `5e1800e`，本轮无服务端代码变更。详情见 [持续比较](../specs/continuous-contract-evolution.md)。

下一轮：先以复杂条件和跨实例冲突补充语义验收，继而推进执行包的 skill/资料取得及目标 Agent 实际读取和交付，不能把“导出文本”视为“已经执行”。

## 第 6 轮：条件、冲突与具体采纳（已打包部署验收）

新增三类合成真实模型端到端：条件与本期例外、两个实例的长期修改冲突、仅采纳部分 Agent 建议。前两例首轮通过，第三例依次暴露服务失败、覆盖含义混淆及混合来源归因过粗。完整失败链、原始响应与 SHA-256 均保留，不改写答案抬高结果。

明确无新增要求仍算处理过的事件；新增 evidenceSchemaVersion:1/SourceRef.CONTEXT，区分用户具体采纳的直接依据与 Agent 原提议背景。未标明角色的混合发言继续要求审阅，只有背景/伪造引用仍拒绝；旧请求不启用新格式。同一未采纳决定只显示具体问题，避免程序兜底重复提问。

新版部分采纳真实运行 `2026-09-22T19-04-49-118Z` 通过；最终三例回放 `19-14-18-849Z` 通过，含冲突先阻断、明确合成用户裁决、确认新版并创建新实例。178 项全量测试通过。三类旧流程及主动/持续增量回放亦通过。12 次真实模型调用尝试，11 次已知 124,672 tokens，另一次用量及费用未知；回放零模型调用。详见 [语义验收](contract-semantic-trials.md)。

源码 `46b2c1a` 已干净打包，ZIP 解压客户端三例通过 `2026-09-22T19-24-58-636Z`，签名校验通过。试用包 `release/Worket-loop-46b2c1a.zip`，SHA-256 `610d1f31455429726dbe6ffe399499f90c988c8cff3ad98e9c41a2ed1500f755`。后台同版已部署；公网健康 200、匿名能力 401、admin 404，认证能力 200 且 rule/evolution/evidence schema 1。备份及回滚见部署记录。未替换用户安装或发布 GitHub Release，系统钥匙串批准仍未验证。

随后优先修复已复现的 MCP 旧交接包缓存问题：当前工作新增资料后，get_work_context 仍返回旧 neededArtifacts。复现已保存；尚未修复，不宣称 Agent 已读取最新工作包。后续继续必要 skill/资料取得和实际执行验收。

## 第 7 轮：MCP 读取当前工作包（已打包验收）

修复 get_work_context 复用旧快照的问题，每次读取生成当前工作包、复查固定资料和文件输入，通过后才记读取回执；旧快照不变。旧解压客户端在同一端到端脚本稳定失败，新源码真实 Electron + HTTP MCP 通过，180 项回归通过，零模型调用。详细成功/失败与边界见 [当前上下文验收](mcp-context-freshness.md)。

源码提交 `bc5aaac`，干净导出构建的 ZIP 解压客户端通过 `2026-09-22T19-33-10-970Z`；原包和解压签名通过。试用包 `release/Worket-loop-bc5aaac.zip`，SHA-256 `030741b4ef8df9d8646c7c903159cb908a310e6b02a51602a91169c50416e558`。未替换用户安装，未发布 GitHub Release；正常系统钥匙串授权仍未验证。后台维持 `46b2c1a`。

下一轮重点：目前 `DefinitionContent.methods` 能表达 REQUIRED 方法，`materialRoles` 只能绑定单文件，缺少 skill/执行依赖的明确引用、附属文件和可用性检查。先确定保持“要求 / 执行方法 / 本次输入”分离的最小模型及失败案例，再完成新实例执行依赖取得和目标 Agent 真实读取交付。不要仅把复制 SKILL.md 或导出路径算成依赖齐全；不自动扫描/上传无关本地 skill。

## 第 8 轮：技能依赖与本次输入边界（已打包部署验收）

增加 SKILL 资料角色，显式绑定完整技能目录，保留配套文件、空目录与可执行位；固定副本按清单 hash 版本化，所有工作包出口重新校验。原技能删除不影响旧实例，更新技能产生新版本。目录外运行依赖仍需核验。

真实模型首轮发现历史输入值重复、提炼指令污染约定，修正角色与值、元指令与执行约束的提示后同源复验通过。失败原文保留；4 次模型调用已知 38,233 tokens，费用未知。成功真实响应接入完整 Electron/HTTP 回放→绑定技能→发布→新实例→MCP→QA 配套脚本→合成验收通过。185 项全量测试、技能专项和旧语义回放通过。详见 [技能依赖验收](skill-dependencies.md)。

源码 `58be614` 已干净打包、部署。ZIP 解压技能链路通过 `2026-09-22T20-00-35-466Z`，MCP 回归通过 `20-00-49-488Z`；签名通过，公网四类能力 schema 均为 1。试用包 `release/Worket-loop-58be614.zip`，SHA-256 `e8524b1d588d12f0517863b09b8161e6561d52c155b1ef5f15153df286ac70a9`。打包测试隔离钥匙串，正常系统授权未验；未替换安装或发布 GitHub Release。

下一轮优先复现/修复读取回执的交接归属：MCP 只传 work_id，#recordToolReadSuccess 直接更新 pending_dispatches.read_at；AppService.handoff 生成新 handoff.id / Binding，但未见读取证据重置。需证明换执行者后旧读回执不会被继承、旧 delivery 不能确认新接手，再验证目标读取路径。入口：src/bridge/mcp-handler.ts、src/app/app-service.ts、src/distillation/desktop.ts、src/executors/work-bootstrap.ts；现为代码审计发现，尚未完成新交接的独立复现。跨技能/插件依赖继续保留，不把 QA runner 产物算成用户真实业务交付。

## 第 9 轮：本次交接的读取回执（已打包验收）

独立复现第二次交接继承第一次读取，修复为 deliveryId/Binding 独立回执。只有当前标识读取加真实会话绑定才确认接手，读取可先于 Hook；仅查看、旧标识不确认。取消/失败恢复原会话证据，旧 pending 升级不信全局标志。

188 项全量测试通过；两次交接真实 Electron/HTTP 与 MCP 当前资料、技能复用回归通过。新增模型调用为零，外部 Agent 边界受控，未宣称真实业务交付。完整失败与通过见 [验收](delivery-receipts.md)。源码 `7ead4f4` 已干净打包，ZIP 解压的回执、MCP 和技能链路全部通过；签名、SHA-256 和运行号见专项验收。后台维持 `58be614`。

## 第 10 轮：稳定来源与内容修订（已打包验收）

复现 Codex 流式半句/工具运行状态在最终完成后被同 ID 去重吞掉。现在延迟运行中非用户内容入档，并按实际会话追加同 ID 修订，保留原文和旧工作包。有效视图保持消息原顺序，用于当前状态、MCP、提炼和持续比较；过时自动派生项隐藏、提炼失败重启补算，手工编辑保留；拆分点映射原应用 ID。

193 项测试通过；新记录、旧半句修复、交接与持续提炼桌面回归通过。旧 7ead4f4 解压客户端同脚本确实失败。所有新模型调用为零，失败脚本和原复现保留，见 [验收](source-revisions.md)。源码 `a971268` 已干净打包，原包/解压签名和四条解压端到端全部通过，包与运行号见专项验收；后台维持 `58be614`。消息删除/分支回退与远端改进样本修订关系保留为后续缺口。

## 第 11 轮：来源分页完整性（已打包验收）

真实只读对照确认用户引用任务旧全量读取当前完整；8 页读取后的 774 条规范化事件 hash 相同。受控摘要历史暴露旧客户端漏末页。现在完整加载回合/消息，只有显式不支持方法才兼容回退，缺页/游标循环/冲突/错误均不写半份记录。

198 项全量及 Electron/真实 stdio 端到端通过，旧解压客户端同一受控场景失败。无新模型调用、无任务写入。见 [验收](codex-history-completeness.md)。源码 `e794fd3` 已干净打包，原包/解压签名、分页及来源修订两条解压端到端均通过；后台维持 `58be614`。包 hash/运行号见专项验收。分页本身不是原子快照，不据此推断消息删除或分支回退。

## 第 12 轮：来源缺失、待复核与恢复（已打包验收）

旧客户端在消息消失后继续采用旧要求，已用同场景解压客户端复现。完整来源第一次缺失进入待复核并暂停新交接/提炼；第二次一致完整读取才排除当前内容，恢复则追加新观察。已确认约定、人工编辑、历史交接包保持；显式起点丢失不扩大采集范围，允许用户重新选择。

204 项回归和来源/交接/持续比较端到端通过；没有新增供应商调用。完整性证明仅用于 Codex 与支持的新 WorkBuddy，未知旧接口不推断。详见 [验收](source-presence.md)。源码 `9fd7124` 已干净打包，原包/解压签名、来源缺失/交接/持续提炼三条解压端到端均通过；后台维持 `58be614`，包 hash 与运行号见专项验收。

## 第 13 轮：改进上传与删除的实际发送边界（已打包验收）

真实 HTTP 及旧解压客户端复现停止后仍 POST；现在等待连接后、真正发出前重查授权、订阅、有效期和原服务身份。删除同样不随服务切换，退出后仍能回原服务删除；能力查询期间停止再开不会复活待定授权。207 项测试及双后台 Electron 设置路径通过。零模型调用，不变更采集范围或后台协议，见 [验收](improvement-send-boundary.md)。源码 `f1ea25c` 已干净打包，原包/解压签名、双后台发送边界和持续提炼两条解压端到端均通过；后台维持 `58be614`，包与 hash 见专项验收。

## 第 14 轮：改进样本的有效原文视图（已打包部署验收）

复现后台把修订前后要求并列、遗漏缺失关系。新增 v2 记录样本视图，完整分片、覆盖和修订关系齐全才供当前使用；历史原文保留，修订保持会话位置。旧后台暂停队列，新后台兼容旧消息但明确有效性未知。210 项测试、真实 Electron/stdio/HTTP/管理员路径与发送边界回归通过，零模型调用。见 [验收](recording-sample-view.md)。源码 `5eecf20` 已干净打包及后台部署，原包/解压签名、来源到管理员与发送边界两条解压端到端通过；公网健康/能力及运行 hash 确认。见专项验收及运维记录。

## 第 15 轮：长记录增量同步与双预算（已打包部署验收）

实测完整视图使 200→400 消息上传近四倍增长，550 消息在旧限额仅同步到 499 条。改为持久基线加视图增量，消息及历史保留；记录样本改为 10,000 事件/20 MiB 双预算。400 条可比负载上传减少 94.31%，550 条离线/重启及真实 Electron→stdio→HTTP→管理员路径完整通过。213 项回归通过，零模型调用。见 [验收](recording-sample-scale.md)。源码 `44dd3ba` 已干净打包部署；解压 550 次刷新验收通过，原包/解压签名、公网能力和运行 hash 确认，详情见专项验收与运维记录。

## 第 16 轮：每次工作的文件版本（已打包验收）

同一路径 SCRIPT.md 两次创建后，旧实例误读第二期脚本的问题已用上一版 44dd3ba 解压客户端复现。新建实例固定 FILE 输入和参考案例的独立副本；显式更换文件有 hash 防陈旧与幂等命令，保留旧副本/来源，所有包出口重新校验。历史输入无快照时须明确重选；参考按创建 hash 确认或移除。修改后重置本实例全部读取回执，旧交接包保持原版本。

217 项全量测试、源码 Electron 创建两个版本/两次输入/特殊要求隔离、原文件删除、换文件与 HTTP MCP 重读、legacy 恢复通过。上一版同脚本在串读处失败。供应商调用为零，外部 Agent 边界受控，不宣称新业务交付。详见 [验收](instance-files.md)。源码 c08d9eb 已干净构建，原包/解压签名通过，ZIP 解压实例链路及交接回执回归均通过。后台维持 44dd3ba，无本轮协议变更。包 hash 与运行号见专项验收。

## 第 17 轮：无变化采集成本（已打包验收）

量测发现 3 项各 2,000 条消息，无变化检查仍每轮载入完整档案并重复尝试 6,000 次入队，中位 232.97 ms。新增本地追加 row_id 检查点及查询索引，先核验生命周期和到期，再决定是否扫描；视图/outbox/检查点同事务。迟到来源序号不会被当成无变化，部分分片和事务中断可重启补齐。

同负载中位约 0.055 ms，无变化完整快照与入队尝试均为零。221 项全量测试、真实 Electron→HTTP 后台无变化后修订/消失/恢复通过；旧 c08d9eb 同脚本重复读取而失败。局部耗时不外推整体应用性能，零供应商调用。见 [验收](recording-idle.md)。源码 7dd6c4a 已干净打包，原包/解压签名通过；解压客户端无变化、修订恢复与 550 次新增刷新全部通过，后台当前 754 条消息完整。后台维持 44dd3ba。

## 第 18 轮：固定资料精确恢复（已打包验收）

旧客户端在真实“本次文件”重选正确原件时仍使用损坏副本，已复现。新增显式“修复固定资料”，核对文件完整 hash 或技能完整清单，错误版本不写入，恢复原路径/原 ID/原版本。普通输入重选相同内容也可修复；目录异常切换回滚，外部路径及替换链接拒绝。

225 项全量测试、源码输入重选与定义/实例恢复→MCP 两条端到端通过，旧客户端同场景失败。零模型调用，不宣称业务交付或断电原子性。见 [验收](material-recovery.md)。源码 db8dfa2 已干净打包，签名及两条 ZIP 解压端到端通过，默认窄面板截图已核对；后台维持 44dd3ba。

## 第 19 轮：取消强制、禁止和本次豁免（已部署验收）

新增三种义务变化的真实模型端到端，识别测试否定词误报与实际模型把通用覆盖写成 condition 的失败，分别保留并修复。新版提示明确通用覆盖和真正限定条件，范围校验不放宽；禁止真实复验、平台条件真实回归、新旧实例及实际验收列表通过。

230 项测试通过；10 次真实供应商调用、已知 85,565 tokens，费用未知。三例现有 db8dfa2 ZIP 解压客户端回放通过，客户端运行时代码不变。后台 b4702a0 已干净构建部署，公网健康/能力和三份运行 hash 均通过。详见 [验收](obligation-evolution.md)。

## 第 20 轮：候选聊天来源变化审阅（已打包验收）

旧客户端已有候选未提示其原文已修订，真实桌面复现。新增本地提交原文/当前内容对照及明确确认，发布事务核验最新变化集合；保持旧快照与历史实例，新增未选消息不触发确认。233 项测试、默认窄面板后台再次修订→旧确认失效→重新确认→复用，以及真实 stdio 缺失/恢复/发布链路均通过。零供应商调用，三种既有义务回放通过。源码 657c2a6 已干净打包，原包/解压签名、两条解压端到端及窄面板截图核对通过。后台保持 b4702a0。见 [验收](source-review.md)。

## 第 21 轮：重复范围与相同关系归一化（已部署验收）

同实例重试、多个明确本次要求实际模型链路保持 v1，明确长期提升原本因相同关系写在两个字段而被拒绝；后台入口仅折叠一致重复，规范校验保持。原始正确响应回放走通 v2 与新实例。一个基准含糊的场景保留 FAILED，不伪装全通过。239 项测试和现有 657c2a6 解压客户端两条回放通过；8 次真实调用、90,803 tokens，费用未知。后台 8d39223 已干净构建部署，公网健康/认证能力及三份运行 hash 一致。见 [验收](repetition-evolution.md)。

## 第 22 轮：草稿资料与手动修订恢复（已打包验收）

旧 657c2a6 包复现“已暂存”后资料选择丢失。暂存改为固定文件/完整技能副本，草稿与定义共享所有权；手动修订恢复未发布草稿。245 项测试、重启/删除原件/发布/新实例/MCP/合成技能执行及资料恢复回归通过，零供应商调用。源码 325e7ec 已干净构建，原包/解压签名、草稿全链路与资料恢复两条解压验收通过，窄面板截图已核对。详见 [验收](draft-materials.md)。

## 第 23 轮：整体采纳的局部例外（已验收）

两例预注册真实模型试验正确保留长期安全区/片尾来源，排除金色/1:1 临时要求；第二例原检查器漏识别“距画面边缘”而 FAILED，原记录保持。修正等义检查后，同一未修改响应在 325e7ec 解压客户端两条全链路通过；247 项测试。4 次真实调用、61936 tokens，费用未知；无需运行逻辑变更或部署。见 [验收](bounded-adoption.md)。

## 第 24 轮：审阅同名条目隔离（已打包验收）

删除方法误清空同 key 资料选择的问题在 325e7ec 解压包复现。修复为按栏位身份清理；源码完整草稿/MCP 链路和 247 项测试通过，零模型调用。源码 ff60eb1 已干净构建，原包/解压签名、同名条目隔离和完整后续链路通过；窄面板截图核对，后台维持 8d39223。详见 [草稿验收补充](draft-materials.md)。

## 第 25 轮：暂存与撤销的固定版本边界（已打包验收）

旧 ff60eb1 解压包复现撤销方法后重新暂存会把固定模板 v1 升级为原件 v2。成功暂存现在清空旧撤销快照，失败保留。247 项测试及失败保存、版本保持、重启/新实例/MCP 全链路通过；零模型调用。源码 84c5480 已干净构建，原包/解压签名和解压完整链路通过，窄面板截图已核对；后台不变。详见 [草稿验收](draft-materials.md)。

## 第 26 轮：组合技能的固定目录关系（已打包验收）

旧 84c5480 解压包复现两个已选技能的兄弟相对引用失效。新工作包按定义版本组装共同目录，保留原目录名与文件内容，未选依赖不收集。252 项测试、源码 Electron→MCP→跨目录合成模块执行、依赖更新后的新旧实例隔离、同名冲突回滚和损坏重建通过；单技能回归通过，零模型调用。源码 bff67e2 已干净构建，原包/解压签名、组合技能和单技能两条解压链路通过，窄面板截图已查看，后台保持 8d39223。见 [验收](combined-skills.md)。

## 第 27 轮：规范文件部分采纳与本次例外（已验收）

两例真实模型正确保留已采纳条款与聊天补充，排除 Logo/音乐及本次 12px。原检查器将独立 SKILL 角色误当规范重复绑定，两份 FAILED 原报告保留；修正检查器后同一响应在 bff67e2 解压客户端两条完整链路通过。254 项测试，4 次真实调用 / 48065 tokens，费用未知。无需改运行逻辑、打包或部署。见 [验收](document-adoption.md)。

## 第 28 轮：规范修订的审阅范围（已打包验收）

旧 bff67e2 包复现更新字数条款清空无关输入确认，现只重开直接条款/栏位整体决定，关联规则仍复核。空白行失败未证实来源残留，未改快照事务设计。255 项测试及重启、移除原件、正确行号发布/新实例/MCP 全链路通过，零模型调用；夹具共享对象错误保留。源码 903263b 已干净构建，原包/解压签名、规范修订及两例文件采纳回归全部通过，窄面板截图已核对；后台保持 8d39223。见 [验收](document-revision-review.md)。

## 首批试用包与部署（已验收）

源码 `858c677`，从 git archive 干净导出构建，未包含工作区未提交实验材料。试用包 `release/Worket-loop-858c677.zip`；SHA-256 `7422b4737141417f4e3b69824afaafd365ac63c75a3dcd4387d9ee7bda7a6ed7`。原包与 ZIP 解压后的签名通过，解压客户端三类完整桌面回放通过：video `2026-09-22T17-26-18-552Z`、report `17-26-29-499Z`、code `17-26-34-082Z`。打包测试采用隔离测试钥匙串，未验证正常系统钥匙串授权；版本标识仍为 0.1.4 的本地试用包，未发布 GitHub Release 或替换 Applications 中的安装。

后台已更新至 `/opt/worket/releases/858c677`，配置、数据持久化与安全参数保持。已校验运行文件 hash 与 server 构建相同（桌面构建另含 sourceMappingURL）；公网 HTTPS health 200、未授权 capabilities 401、admin 404。回滚与备份见 [部署记录](../deployment/jp-server-worket.md)。

本轮 12 次桌面运行记录见 [机器结果](worket-improvement-loop-results.json)。模型响应均为冻结回放，实际供应商调用为零。第一次评测工具测试被沙箱阻止本地监听，取得执行权限后同一套 34 项通过。

## 续跑约束

- 保留现有 AGENTS.md、BP B008 桌宠修改、两份实验 ZIP 与 worket-extraction-loop/，不将其混入本轮提交。
- 额度起点：账户周额度已用 38%，此数为检查当时快照；后续按实时工具查询。未经明确授权不兑换重置次数。
- 历史全部 16 次运行仍见 working-contract-v2-results.json。本轮只冻结其中五份有代表性的原始响应，未删除其他失败。
- 当前试用包源码为 903263b，后台为 8d39223；后续文档提交不改变构建身份。
- 最新额度检查为账户周额度已用 91%；不兑换重置次数。
- 第 28 轮完成：255 项测试，零模型调用，规范修订保留无关决定并重开直接/关联事项，重启/删除原件/行号/发布/MCP 和文件部分采纳解压回归通过。下一轮预注册跨实例明确纠正试验：同一约定基准下，前一实例明确提出新长期阈值，后一实例明确引用并撤回该阈值、统一改用另一个值；应保留最终明确纠正，不因跨实例一概视为未决冲突，也不能仅按消息先后覆盖。配对检查 Agent 自行重述/建议不等于用户撤回。复用 qa-evolution-semantics 的真实模型/解压链路，先固定输入及预期、每例最多两次调用；保留与已验证的 cross-instance-conflict 区别、原失败及检查器错误。尚未执行该试验。
- 最近包 903263b；/tmp/worket-document-revision-extracted-path.txt 指向解压客户端，/tmp/worket-document-revision-package-result.json 保存 hash。上一版 bff67e2 解压副本保留；84c5480 及更早解压已清理，可从 ZIP 重建。新打包脚本不再额外复制 release/unpacked，已清理本轮 build；全部 ZIP/output/用户材料保留，下一次打包前检查磁盘。

- 记录有变化时仍载入完整 WorkSnapshot；局部无变化性能改善不等于所有本地开销已消除。多个明确选定技能的本机目录关系已解决；未选择的技能、插件/系统环境、跨机器传递和外部 Agent 实际业务交付仍是未完成边界。
- 本轮磁盘满已清理自己的旧临时构建/解压副本；保留 release ZIP、output 证据及用户实验材料。后续及时回收本循环可重建缓存，避免再次耗尽空间。
