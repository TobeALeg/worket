# 技能执行依赖验收

2026-09-23。技能目录绑定、固定版本、新实例可取得和完整性校验已完成源码及解压客户端验收。不是外部目标 Agent 接手或实际业务交付。

## 真实模型与失败保留

首次合成来源运行 `2026-09-22T19-46-57-123Z`：正确提取 Report Builder 为必需 SKILL，且没有把 csv-cleaner 当长期技能；但额外生成含历史“城市交通”的 current_topic 输入，并将“不把临时工具固定下来 / 不把技能步骤当约束”的提炼指令写成执行约束，判为失败。完整原始响应保留。

修正 RULES 提示对“输入角色 vs 本次值”“提炼指令 vs 真正执行禁令”的定义，同一输入 `2026-09-22T19-50-09-068Z` 通过：一个通用主题输入，Report Builder 作为独立技能角色，csv-cleaner 不进入长期依赖，也没有额外提炼政策约束。验收增加 input.text / choices / defaultValue 检查，没有放宽原断言。4 次真实调用，input 11,020 / output 27,213 / total 38,233 tokens，费用未知。

两例请求、响应、用量、wire、结果及 SHA-256 固定于 test/fixtures/skill-extraction。已调试样本不能再当盲测；这一次修复不能证明跨业务语义准确率。

## 桌面端到端

`2026-09-22T19-55-23-756Z`：将成功样本的原始响应严格回放到真实 Electron + 本地 HTTP，依次选择来源、确认发送范围、生成候选、选择技能目录、确认发布、新主题新实例、认证 MCP 读取、从固定目录运行合成脚本、关联产物并逐项验收为 COMPLETED。输入/中间数据严格匹配，不改写模型答案，零新增模型调用。

合成技能含入口、相对模块、参考文件、可执行位和空目录；删除原目录后配套脚本仍能运行，删掉副本参考文件则 MCP 返回 MATERIAL_MISSING。执行该脚本的是 QA runner，不冒充外部 Agent，也不算用户真实业务验收。实际候选界面和合成验收截图已保存并检查。

人工基准的修改→新版本→复用分支通过 `19-44-28-370Z` 和 `19-50-38-468Z`。两次测试自身失败保留：`19-43-07-362Z` 未关闭参与改进，隔离客户端无服务凭据，未能创建工作；`19-53-50-502Z` 异步等待未取得终态即检查，现显式轮询 API 并断言结果。开发中一次重复 JS 声明造成模块加载失败，修复后全量通过。

## 回归与边界

185 项全量测试通过。随后增加入口目录别名兼容，技能专项 4 项通过；目录内部仍拒绝符号链接，不扫描/复制其他技能。包含缺失/损坏/额外文件阻断、旧实例使用旧副本、新版本重绑、未变副本不创建重复版本、输入值语义失败保留和旧后台发送前拒绝。条件、冲突、部分采纳三例桌面回放 `19-47-04-393Z` 全通过。

工作包可取得显式选定目录，不保证目录外插件、其他 skill、权限或运行环境已经就绪；尚未验证目标执行者真实接手及业务成效。下一步继续实际执行依赖和接力路径。

## 打包与部署

源码 `58be614` 干净导出构建；试用包 `release/Worket-loop-58be614.zip`，SHA-256 `e8524b1d588d12f0517863b09b8161e6561d52c155b1ef5f15153df286ac70a9`。原包和 ZIP 解压签名通过。解压客户端完整技能链路通过 `2026-09-22T20-00-35-466Z`，MCP 当前包/损坏资料回归通过 `20-00-49-488Z`。使用隔离 mock keychain，正常系统钥匙串批准仍未验证。未替换用户安装或发布 GitHub Release。

后台同版已部署，health 200，未认证能力 401，admin 404，认证能力 200 且 rule/evolution/evidence/skill schema 均为 1。没有数据库迁移、模型配置或凭据变更。备份及回滚见 [部署记录](../deployment/jp-server-worket.md)。部署后不重复模型调用。
