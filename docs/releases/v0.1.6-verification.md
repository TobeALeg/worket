# Worket v0.1.6 发布验收

## 源码与公开附件

- 构建提交和标签 `v0.1.6`：`71a4f941e93a4ad72c88b910b0e4ee4ff0faf806`。
- [PR #7](https://github.com/TobeALeg/worket/pull/7) 按固定 HEAD 合并，main 合并提交为 `1ae13716e74cceaf6d8b3dd672971ba3e505a843`，内容与发布构建一致。仓库无远端 CI 检查，本机完整发布验证通过。
- 从干净 worktree 构建，安装包扫描没有本地 output、研究材料、数据库或配置秘密；过滤模块与构建产物字节一致。
- [正式 Release](https://github.com/TobeALeg/worket/releases/tag/v0.1.6) 已公开为 Latest，非 draft、非 prerelease，发布时间 `2026-09-24T11:02:56Z`。
- macOS arm64 ZIP：`Worket-0.1.6-darwin-arm64.zip`，134,336,103 字节；ZIP 与 `.sha256` 均为 uploaded，远端附件摘要与本地一致。
- ZIP SHA-256：`69b3718691edd88ba9c044281d65e99f4a39d0d285520aa8e16356a41cda9d77`。

从官方 v0.1.5 app.asar 提取原更新读取器，匿名请求 GitHub `releases/latest` 后正确发现 v0.1.6，并通过原 downloadRelease 完整下载 134,336,103 字节公开 ZIP，SHA-256 与上述附件一致。用户无需 GitHub 登录；报告为本地 `output/public-v0.1.6/report.json`。

## 回归与跨版本升级

`npm run release:mac` 完整成功：294/294 测试、打包、ad-hoc 签名、ZIP 解压后二次严格验签、`qa-electron.mjs` 与 `qa-updates.mjs` 均通过。日志为本地忽略目录 `output/release-v0.1.6-build.log`。签名不是 Apple 公证。

官方 v0.1.5 → 最终 v0.1.6 ZIP 的隔离升级检查首次在新应用的调试连接阶段超时，失败日志保留为 `output/release-v0.1.6-upgrade-attempt-1.log`。未修改 ZIP 或测试预期，第二次完整运行通过：工作 ID、标题、完成状态、两条来源原文、执行片段和改进设置保持，数据库 quick_check=ok。报告见 `output/v0.1.6-upgrade-qa.json`；此为合成数据的升级兼容验证。

长记录过滤的真实模型结果与批量人评沿用 [原失败记录验证](../acceptance/distillation-input-filtering.md)，本轮发布验证没有新增模型调用。原生模态更新弹窗可能阻塞 Hook 的旧问题仍未修复，不能将更新状态可视化描述为该问题已解决。

## 生产后台

已从 `/opt/worket/releases/8ffa545` 切换到 `/opt/worket/releases/71a4f94`。新能力 `analysisSchemaVersions:[1]` 与原自动准备、规则、演进、证据、技能能力均通过认证接口核验；公网 health 200/configured、匿名 capabilities 401、admin 404。

部署前无活跃或待领取请求。离线模块导入、数据备份、原子目录切换及失败回滚路径已准备；本次没有触发回滚。原 User、镜像、环境、挂载、网络与安全配置逐项保持；20 条请求记录保留，settings 与加密/签名密钥逐字节一致，两份 SQLite quick_check=ok，没有数据库迁移。

后台包 SHA-256 为 `3cbb347348acdc1673c5d053f6f1f0a5cdf8d03d03e93b7a5489ef30ebf56c5f`。备份 `/opt/worket/backups/data-pre-v016-20260924-185319.tgz`，回滚容器 `worket-before-v016-20260924-185319` 保留。代码模块摘要与构建一致，完整结果见 [生产验证记录](v0.1.6-production.json)。检查令牌只在服务器内存使用，没有输出或传出。

## 本机实际安装

用户原来运行的是工作区发布目录中的 v0.1.5，`/Applications` 还保留 v0.1.4。已正常退出旧进程，将最终 ZIP 的 v0.1.6 安装到 `/Applications/Worket.app` 并启动；lsof 确认运行文件来自该路径，Info.plist 为 0.1.6，app.asar 与最终 ZIP 字节一致。

更新前备份旧安装与工作数据库。安装后原有 4 项工作、1,573 条来源事件、16 份快照、4 份候选、1 份定义和 7 项沉淀任务计数均保持，旧工作 ID 全部保留，quick_check=ok。本机桥接在实际新端口完成认证 MCP tools/list，HTTP 200、17ms，四个工作读取工具可用。备份位于 `output/local-v016-backup/`，安装核验报告为 `output/v0.1.6-installed.json`，均不提交私人内容。
