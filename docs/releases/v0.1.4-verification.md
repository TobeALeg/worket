# Worket v0.1.4 发布验收

日期：2026-09-18。发布提交：`78d09d4809371798b66221c4181387b27bd392f8`。GitHub Release：<https://github.com/TobeALeg/worket/releases/tag/v0.1.4>。

## 安装包

- `Worket-0.1.4-darwin-arm64.zip`
- 大小：`132017287` bytes
- SHA-256：`6d3237c7ffc4ca0118d2854888d5e012ec8067d9753a84bd986369b7e4a93b03`
- GitHub 远端附件状态为 `uploaded`，远端大小与本地一致；独立 `.sha256` 文件同时发布。

## 自动化与打包

- `npm test`：125/125 通过。
- `npm run release:mac`：构建、ad-hoc 签名、ZIP 解压后二次签名校验通过。
- 打包 Electron QA 通过：Dock、第二实例恢复、桌宠状态、400×660 面板、授权文案、HTTP 下载、校验和及手动替换更新流程。
- 从发布 ZIP 安装到 `/Applications/Worket.app`，`CFBundleShortVersionString=0.1.4`，`codesign --verify --deep --strict` 通过；旧 v0.1.3 暂存于 `/Applications/Worket.app.v0.1.3-backup`。

## 托管服务闭环

- `jp-server` 运行同一提交，`/opt/worket/current` 指向对应不可变版本目录。
- 后台设置由 schema v1 迁移到 v2；升级前数据备份为 `/opt/worket/backups/data-pre-v014-20260918.tgz`，权限 `0600 root:root`。
- 容器启用自动接入、可信本机代理来源头和全局每日 200 次模型调用上限；公网 `/health` 返回 `configured:true`，匿名 `/v1/capabilities` 返回 401，公网 `/admin/` 返回 404。
- 正式版首次启动后服务记录 1 个匿名用户和 1 台有效设备。桌面“Worket 服务”显示 `https://worket.dandi.site`、稳定用户 ID 和“已连接”；连接检查不触发模型调用。

## 未扩大的结论

本轮没有使用用户真实工作记录执行沉淀，因此不把连接成功写成真实模型候选质量、定义发布或复用收益验收。恢复码原文未读取或复制；跨设备恢复、上传归属、删除和设备撤销由自动化测试覆盖，真实第二台设备仍待实际使用验证。
