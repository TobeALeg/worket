# Worket v0.1.5 发布验收

## 源码与附件

- 构建提交：`8ffa5458cb4a0e7b841c802f97b0047e6a84cd57`，已推送 `main`；版本标签 `v0.1.5` 指向该提交。
- 从干净 Git worktree 构建，未包含本地未跟踪材料、工作数据或凭据。
- [正式 Release](https://github.com/TobeALeg/worket/releases/tag/v0.1.5) 已公开并设为 Latest，非 draft、非 prerelease。
- macOS arm64 ZIP 为 `Worket-0.1.5-darwin-arm64.zip`，132,889,638 字节；ZIP 与 `.sha256` 均为 `uploaded`，远端附件摘要与本地一致。
- ZIP SHA256：`13971de4a31bc2a29e1b9183b381045f09a9fc15496d5c44002230c482bed15d`。

## 测试与升级

完整测试 265/265 通过。发布包完成 ad-hoc 签名、ZIP 打包和解压后二次严格签名验证。

`release:mac` 初次在解压应用的 Playwright 调试连接阶段超时，未把此命令记为完整通过。同一 ZIP 重新解压后，在本机既有测试环境运行 `qa-electron.mjs`、`qa-updates.mjs` 均通过，期间未重建或修改 ZIP。日志见本地 `output/release-v0.1.5-{build,electron,updates}.log`。

以公开 v0.1.4 ZIP 创建隔离工作库，再用 v0.1.5 打开同一目录：工作 ID、标题、完成状态、执行片段、两条完整来源和已关闭的改进偏好均保留；`quick_check=ok`。通过实际界面打开旧工作，并验证设置返回后仍停留在该工作。报告见本地 `output/v0.1.5-upgrade-qa.json`。

从 v0.1.4 官方包提取原更新读取器，以匿名请求读取 GitHub `releases/latest`，正确发现 v0.1.5，并通过原 `downloadRelease` 下载完整公开 ZIP；SHA256 与上传文件一致。报告见本地 `output/public-v0.1.5/report.json`，确认公开用户无需 GitHub 登录即可完成发现与下载。

## 后台边界

本次公开的是客户端。生产后台仍运行 `8d39223`，缺少 `continuationSchemaVersions`；客户端将显示升级提示并保留来源，不把未获得的模型状态标为已准备。正式发布说明已明确这一限制。

配套后台包已编译并上传至服务器临时目录；升级脚本包含空闲检查、数据备份、原配置保留、添加 `requests.operation` 字段、健康检查及失败回滚。但自动审批拒绝停止生产容器和迁移数据库，认为推送/发布授权未明确覆盖生产部署；已向用户请求单独确认，尚未执行切换或迁移。后台包 SHA256：`74146ffdddd456389917b4e7140e1388ca00541034ae9ffc53d32124aa513fa0`。

本轮没有真实模型调用、真实记录沉淀或生产账号修改。当前本机应用仍为前轮已安装的同批功能构建，未因公开发布再次重启。
