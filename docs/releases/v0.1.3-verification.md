# Worket v0.1.3 发布验收

发布时间：2026-09-18 13:29（Asia/Shanghai）。

- 正式 Release：[v0.1.3](https://github.com/TobeALeg/worket/releases/tag/v0.1.3)
- 发布标签：`v0.1.3`，构建提交：`7c72ab6`（`发布 Worket v0.1.3`）。
- GitHub 已确认 `draft=false`、`prerelease=false`，`releases/latest` 指向本版本。
- 发布重点：ZCode 与 Antigravity 本地历史适配器、记录同步、交接待确认回执，以及持久化的改进采集关闭偏好。

## 安装包

`Worket-0.1.3-darwin-arm64.zip`：132035439 字节。

SHA256：`4a1b96b7e02fc0bd53d4bd5d37fb854b0d92f84ec7e0698cfea87d5b8415496d`。

随附 `Worket-0.1.3-darwin-arm64.zip.sha256`；两个附件均已上传并可通过公开 Release 下载。安装包采用 ad-hoc 签名，未经过 Apple 公证。

## 验证

- `npm run release:mac`：125 项测试全部通过。
- 通过 macOS arm64 打包、ad-hoc 签名、解压后的严格签名验证。
- 通过真实打包应用的菜单、桌宠、面板、重复启动恢复和更新下载/校验 QA。
- `qa-new-executors` 的本机历史选择与交接界面验证已在发布前完成；不包含真实模型接力，不把合成回执当成真实模型执行证据。

## 边界

当前原 VPS 已退役；Worket AI 服务需要单独配置可用的服务地址和接入令牌。桌面端普通本地记录不依赖该服务，沉淀和改进采集的远端能力依赖兼容的后台版本。
