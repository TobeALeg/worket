# 产品定义：工作记录、接力与沉淀复用

## 2026-09-23：从后续交互更新约定

最新约定新增“从后续工作更新”：用户选择同一约定系列下的工作并确认范围，比较尚未确认过的新交互。未变的规则、输入角色和固定资料由程序保留；模型提出明确差异。审阅默认只看变化，可展开完整约定和重复/单次要求依据。

重复只增加可追溯依据，不增加相同版本；确认有效变化后新版本用于下一次实例。旧实例保留旧规则，过期基准不能覆盖新版本，来源删除仍脱敏。已通过一次两轮真实模型合成场景和桌面回放，未证明业务节时或普遍语义准确。当前仍由用户主动比较，自动触发尚未启用。详见 [增量比较](specs/contract-incremental-evolution.md)。

2026-09-23 已启动 [持续优化计划](specs/worket-improvement-loop.md)。首轮固定可复现桌面回放与失败样本；回放通过证明已记录结果可走完产品链路，不代表新模型的语义质量或实际业务交付已经通过。

## 2026-09-22：工作约定的规则协调与规范引用

本轮实现 [working contract 通用机制](specs/working-contract-evolution-v2.md) 的首条完整链路：选择已授权交互与规范文本、审阅协调结果、保存定义、新建实例、导出有效工作包及逐项验收。验收证据与范围见 [本轮验收记录](acceptance/working-contract-v2.md)。

同义要求在执行包中只保留一个有效条目，多个来源仍可追溯。长期修正可以替代同组任一重复条目，整组旧要求退出执行；旧证据不会被当成新数值或新义务的依据。补充、长期替代、单次覆盖和未解冲突分别表达；范围不明或未采纳的建议不直接成为以后工作的要求。规则被去重后，验收仍引用保留下来的规则；无需为了验收再维护一份同义文字。

附件默认是参考。用户可将选中的文本设为规范或本次输入；只有实际读取且明确设为规范的条款可以成为文件规则。定义引用条款的固定内容版本，Worket 保存不可变快照；交付给 Agent 时展开选定条款，原件移动或删除不会让本地路径变成失效要求。非文本资料仍需另行传递。

手工修改规则和更新规范条款都会让受影响的直接及间接关系重新待审；旧确认不会沿用到改变后的要求。审阅页在保存时显示新增待定项，确认前不发布。

“修订”内可检查/采用规范的新版本，选择明确的条款范围；依赖该条款的关系转为待确认。确认后的新版本用于新实例，历史实例保持旧版本。新建实例时可输入“仅本次”的规则覆盖，长期定义保持不变。

范围：规范版本更新由用户主动选择，支持 UTF-8 Markdown/文本；没有自动文件监视或源文件写回。持续学习支持用户主动增量比较，尚未启用自动触发。系统链路测试不等于新视频已制作完成，也不证明长期节时或泛化准确率。

> 2026-09-16：新增 ZCode 3.11.2 与 Antigravity 2.14.0 本地历史适配器；主动选择会话后记录已完成的可见消息和工具事件。交付采用复制启动指令、打开应用、用户新建聊天粘贴发送，收到匹配的会话通知后确认接手。Antigravity 源 transcript 截断的工具参数或结果明确显示截断提示；不保存 thinking、系统提示或子 Agent 的私有会话。该功能随 v0.1.3 打包发布；真实模型接力尚待验收。详见 [验收记录](acceptance/zcode-antigravity-executors.md)。

产品优势、竞品比较与待验证洞察统一积累在 [BP 素材清单](bp/benefits-and-insights.md)。其中的候选表述与设计设想不属于本页已交付能力。

> 2026-09-09 已接入执行者注册表：Codex、WorkBuddy 共用记录、同步、交接与复用入口；WorkBuddy 5.5.3 支持直接读取已选会话的可见历史。兼容性与验收范围见 [执行者接入方案](specs/executor-adapters-v1.md)。

> 2026-09-08：本次以 [工作沉淀与复用 Spec v1](specs/work-distillation-v1.md) 为开发依据，已完成开发闭环与合成桌面验证；真实模型、身份接入和真实第二次工作仍待验收，详见 [验收记录](acceptance/work-distillation-v1.md)。下文“用户体验路径”“UX”“当前交付”描述现有记录与接力版本；末尾“下一阶段”描述目标行为，不能据此认定产品已经支持沉淀或托管模型服务。

> 2026-09-18 更新：Worket 托管后台已部署到 `https://worket.dandi.site`，模型供应商、HTTPS、管理页隔离和持久化已经配置。v0.1.4 内置该地址，以匿名用户、恢复码和独立设备身份自动接入；服务器升级和真实桌面公网闭环见 [jp-server 运维记录](deployment/jp-server-worket.md)。

## Production

### 用户是谁

在 Codex、WorkBuddy 等桌面 AI 应用中持续完成真实工作的个人用户。他们的工作散落在多轮 Prompt、Agent 回复、文件和操作中，并且不希望工作上下文被某个 Agent、应用、Skill 或会话锁定。

### 核心价值

用户在桌宠头顶展开的便利贴上点击“记录”后，系统识别前台 Codex 或 WorkBuddy 的工作上下文，把松散的 AI 协作过程记录并抽象为独立的 WorkInstance 和 WorkRecord。记录能够持续跨越不同 Executor 与 ExecutionEnvironment，并在需要时生成可直接接力的 Handoff Package。

产品核心是 Work Object：工作定义独立于执行者。事先定义工作的成本较高，因此从用户已有的真实工作记录进入。记录一方面支持继续同一项工作、交换 Agent，另一方面在用户主动选择“沉淀”后生成可复用的 WorkDefinition，用新输入开展下一次同类工作。未来传递给同事是独立的交接路径；并非所有工作都需要沉淀。

本次不新增 WorkPattern 实体；固定要求与参考方法先在工作定义中明确区分。术语见 [CONTEXT.md](../CONTEXT.md)。

### 产品原则

- 工作属于用户，不属于 Agent、应用、Skill 或会话；
- 用户主动触发后才记录；前台识别只读取应用身份和窗口标题，不读取或保存窗口内容；
- WorkInstance ID 是工作身份，对话 ID 只是来源引用；
- 原始档案与结构化 Work State 分离；
- 每条结构化信息必须可追溯到来源；
- 当前 Work State 的自动提取结果是可追溯的只读投影；用户回到来源对话修正事实后，再触发重新整理。下一阶段的 DefinitionDraft 可编辑，候选编辑不修改原工作状态和记录；
- 前台检测到的原始窗口标题不落盘；仅把来源接口返回的会话标题作为可追溯来源保存；
- 工作记录与定义本地为主，云端处理须明确授权和范围。当前可选客户端模型配置与下一阶段 Worket 后台服务的数据边界不同，见 Spec；
- 完整记录不等于把完整聊天塞进目标 Agent；
- WorkDefinition、WorkInstance、WorkRecord、Executor 和 ExecutionEpisode 必须分离；
- 用户可以取消记录：撤销该项工作的记录授权、停止后续同步，并清除 Worket 保存的本地副本。Worket 不提供删除外部应用原对话的权限或接口。

### 不做什么

- 不创建替用户自主执行工作的 Agent；下一阶段后台仅提供固定的模型分析流程；
- 不读取或推断 Agent 隐藏思维；
- 不持续记录所有桌面活动；
- 不自动判断并切分工作；
- MVP 不支持 Claude、ChatGPT 等其他 Adapter；
- MVP 不实现 WorkPattern 自动提炼与应用；
- 不做完整 Dashboard、工作库云同步、多人协作或全文件历史版本；托管后台只提供匿名用户身份、模型处理、额度和明确授权的改进上传，正式工作库及定义仍以本地为权威；
- 不把 Codex、WorkBuddy 专属字段放入核心 Work State。

### 用户体验路径

1. 用户在任一已接入执行者中开展工作，点击桌宠便利贴“记录”，或从面板“最近活动 / 记录沉睡工作”选择聊天。
2. 能明确识别会话时直接导入；无法定位当前聊天时打开该执行者的会话选择。无标题聊天可以手动选择，不把首次 Prompt 冒充应用标题。
3. 选中会话后立即导入已有可见历史，后续独立同步。Codex 和 WorkBuddy 都支持多项记录；发现聊天不自动保存内容。
4. 查看只读工作状态，按需刷新、从消息新建工作、完成、归档或取消记录。
5. 点击“交接”，选择可用执行者。系统准备工作包并打开目标应用；Codex 打开预填草稿，需要用户在 Codex 确认发送，WorkBuddy 使用新任务 Deep Link。
6. 真实提交带回本次交付标记后绑定目标会话，后续协作写回同一个 WorkRecord，每次切换建立独立执行片段。
7. 未确认交付可以取消并恢复原来源；取消不会关闭目标应用里的草稿，也不会终止已运行的外部任务。确认窗口同时解释好处与风险。
8. 完成或归档后停止同步；继续原工作恢复最后一个真实来源，也可以再次选择执行者交接。

最近活动合并各执行者的最新聊天；历史列表按执行者切换、搜索已加载的标题或项目并加载更早聊天。已有记录显示“打开记录”，重复选择不新建或自动恢复已完成工作。后台每轮同步结束五秒后继续同步所有 OPEN 的真实活动绑定；单个来源失败不影响其他来源，接口读取设置超时。面板刷新保留当前 Tab。

## UX

面板顶部固定显示 Worket 品牌与“最近活动 / 进行中 / 已完成 / 已沉淀”四个 Tab，列表滚动时仍可切换。无项目聊天显示“无项目”：识别 Codex 默认日期目录、WorkBuddy 默认时间目录和用户主目录；真实项目显示目录名，更新时间单独保留。

桌宠圆点表达所有已授权工作的采集状态：有真实活动记录时绿色，只有待确认交接时黄色，否则灰色。面板区分“正在记录 / 等待确认 / 已停止记录”。当 WorkBuddy 不暴露当前聊天身份时，桌宠显示“选择 WorkBuddy 聊天”，点击进入会话选择；不会用某个活动绑定冒充当前窗口。两端都可以并行记录多段会话。

### 设计原则如何回应以上要求

- **一致的品牌形象**：桌宠始终使用与 Dock 图标一致的圆土豆、右上便利贴和微笑表情；空闲时闭眼，其他状态睁眼，同时保留呼吸、搬运、提醒动画及对应体色和状态点，但任何状态都不改变笑嘴；
- **可发现的运行入口**：MVP 保留与桌宠一致的圆土豆加便利贴 Dock 图标；开发中固定使用 `scripts/run-latest.command` 启动当前源码；再次双击应用会把已有桌宠和侧边面板带回前台；
- **单次授权，不逐条打扰**：悬浮桌宠后点击便利贴即授权记录当前工作，不预览全部历史，也不要求逐项确认 Work State；
- **当前来源可见**：只在确认支持的前台聊天后显示窄气泡，标题直接使用来源应用生成的会话标题；
- **提取结果只读**：面板展示来源数量与提取结果，但不提供就地编辑或删除，避免投影与来源事实分叉；
- **执行者可以替换**：接入配置随 Worket 启动安装或更新，交接目标由可用执行者列表提供；
- **小而完整**：采用桌宠加轻量侧边面板，不建立复杂管理后台；
- **隐私可撤销**：完成和归档保留已有记录；取消记录撤销授权并清除本地副本；
- **来源清楚**：Work State 内容可以定位回原始消息，Codex 与 WorkBuddy 内容按 ExecutionEpisode 区分。

## MVP 完成定义

现有记录与接力版本以 [工作流规格](../workflows/record-and-handoff-work.md) 中的十四步真实桌面验收为标准。下一阶段另外必须通过 [沉淀 Spec](specs/work-distillation-v1.md) 中“真实记录 → 用户确认定义 → 新输入 → 新实例 → 外部执行 → 用户验收”的闭环。Mock、静态页面、健康检查或接口成功不能替代真实模型与桌面验证。

## 当前交付

- macOS 桌宠与轻量侧边面板；
- 本地 SQLite Work Core 与八部分 Work State；
- Codex Desktop 真实历史导入、增量 Hook 与 ArtifactRef；
- WorkBuddy 内部只读扩展提供历史、工具记录和增量；通用交接与 MCP 支持同一工作跨执行者继续；
- `OPEN`、`COMPLETED`、`ARCHIVED` 生命周期、继续原工作、来源可追溯的只读 Work State 和取消记录；
- 默认以本地规则提炼 Work State；只有用户在本机显式启用云端提炼并配置 API Key 时才调用 OpenAI-compatible 模型；
- arm64 macOS `.app` 打包与启动时自动、幂等的本机接入安装。

最近活动与历史聊天展示执行者与项目；工作列表和详情展示当前执行者，已结束的工作展示最后执行者。后续增加适配器只需注册，不添加执行者专属按钮或 IPC。

面板按“最近活动 / 进行中 / 已完成 / 已沉淀”四个 Tab 分开，归档记录位于“更多”，默认打开“进行中”。最近活动只展示待记录的来源和“记录沉睡工作”入口，其余 Tab 只展示对应阶段的记录与详情。开始记录后跳转到对应工作阶段，完成或归档后跳转到新阶段；后台刷新保留当前 Tab。

### 桌宠自由移动

桌宠支持按住身体拖到任意显示器，单击仍打开面板；移动超过 5 像素视为拖动，松手不会打开面板。便利贴保持独立点击。位置在松手后保存，重启恢复；显示器移除或分辨率改变时回到可见工作区。

2026-09-11 新增贴边收纳：拖动并松手时，指针距离当前显示器可用区域的左、右或上边缘 24 像素以内即吸附；屏幕底边 Dock 左右空白区域也支持吸附，中央按 Dock 配置估算宽度保守避让。侧边收成 32×68、顶部 68×32 的小头和双爪形态，隐藏气泡与便利贴，保留状态点；单击打开工作面板，拖离边缘恢复正常形态。吸附不影响后台记录。位置与边缘状态一起保存，重启恢复，显示器变化后贴回可见工作区。这里的边缘指桌面可用区域，不跟随其他应用窗口。尚未打包发布。

桌宠角色按原始尺寸的 75% 展示（身体 78 × 67.5 像素），五官、便利贴与点击区域同步缩放；对话气泡保留原字号。

## 用户主动沉淀与复用（开发实现，真实验收待完成）

2026-09-08 决策替代此前“归档为工作副本”的暂定方向：将可复用工作保存的能力命名为“沉淀”，并作为下一阶段核心功能。归档是独立生命周期操作，后续再设计，入口应置于次级区域；历史 ARCHIVED 记录不能自动变成已沉淀定义。

用户可以选择一条或多条已记录工作，点击“开始沉淀”后才触发抽象。系统检查多条记录是否同类，提取输入、交付、约束、验收标准与参考方法，保留来源并提出待确认问题。用户修改、确认后保存定义版本，在“已沉淀”中查看和使用；原记录不自动完成、归档或删除。

复用时，用户提供本次输入，创建新 WorkInstance，交给受支持执行者。旧客户、结论、完成状态与临时授权不默认继承。首次定义的完整价值必须通过第二次真实执行验证；生成候选文本不能单独算作完成。

抽象默认走 Worket 后台提供的模型 API，使用 Worket 的服务端凭据与额度管理，不使用用户的 Codex 订阅额度或 Codex App Server 发起模型任务。现有采集层读取 Codex 聊天的 App Server 不在本次替换范围内。

所选材料会经过 Worket 后台及模型供应商处理，产品不能宣传“全程离线”或“内容不经过 Worket 服务器”。未另行授权时仅暂时处理内容，运行/费用元数据与正文分开；2026-09-09 已实现单次范围授权的产品改进样本与反馈采集，保存 90 天，具体边界见 Spec。用户无需配置模型供应商 API Key；服务登录与用量方案在公开发布前落定。

状态整理、工作定义抽象是两种能力：交换 Agent 和未来传递给同事不触发沉淀。沉淀服务失败不妨碍已有记录、定义和交接材料的本地使用。

### 本次沉淀开发交付

用户在记录上选择“沉淀”或多选来源，在本地范围页明确勾选附件文本内容并确认云端处理后开始。候选按目的、输入、交付、约束、验收、方法和固定资料编辑，用户保存为不可覆盖的版本。固定资料存本地副本；复用时重新提供本次输入，默认不带旧成果。

新工作先保存，显示“尚未交给执行者”；复制或导出不表示接手。目标执行者收到真实 Hook session 与本次交付标记后建立绑定，MCP 读取证据决定完整接手状态。本次成果由用户关联，并逐项选择通过或需要修改；只有全部通过才完成。服务故障不影响已有定义的本地使用。

v0.1.4 的正式版内置 Worket 托管地址。首次使用云端能力时自动创建匿名用户与本设备身份，普通使用者无需接触供应商 Key 或管理员令牌。恢复码可在另一设备恢复同一匿名用户；它不能证明真实姓名、邮箱或企业成员身份。管理员仍可查看用户下的设备、撤销单台设备并控制每用户和全局额度。真实复用验收与商业账号、订阅及组织租户仍未完成。

### 模型服务后台

后台通过浏览器打开，首次设置至少 6 位的管理员密码（本机使用支持简单数字密码），再填写 API 基地址、模型与 Key。“测试连接”只发送固定检测文本，成功后需明确保存；更换配置不需要重启服务。已有密钥不回显，更换供应商地址需重新填写密钥。

模型供应商和调用额度卡片均有“保存并启用”入口，提交同一份配置。修改额度后显示“尚未保存”，提交后在额度卡片内显示成功或失败结果；保存与连接测试进行时，两个保存入口和测试入口统一禁用。

“Worket 接入”生成有期限、可撤销的接入凭据，用户复制到桌面 Worket；“运行记录”显示请求状态和调用次数，不展示工作正文。模型未配置时也能打开后台，发起沉淀会得到明确错误。当前后台只监听本机，远程使用需要单独部署 HTTPS；本机桌面接入使用开发模式。

## 本版本的数据积累目标：改进 Worket

2026-09-09 用户明确：当前版本采集数据的用途是改进 Worket。围绕主动沉淀与复用，积累所选工作材料、模型候选、人工修订和确认、复用及验收反馈，支持功能诊断、质量评测以及提示词、处理流程和模型版本比较。价值来自能复现问题、解释人工修正并验证后续改进的样本链。

改进采集必须作为可见产品能力说明范围、用途、期限及停止和删除方式，不能把现有模型处理授权自动解释为改进留存授权。新工作范围单独确认，未选择的历史聊天、附件和被动桌面活动不自动进入样本库。

**实现状态：**后台“改进样本”支持查看来源、候选原稿、用户修改、确认、复用验收，保存评审备注与删除；可暂停或恢复接收。桌面沉淀范围确认与创建复用工作页面直接展示 `worket-improvement-v1` 采集选项，默认勾选，授权起保存 90 天。2026-09-10 按用户要求改为持久偏好：取消后跨页面和重启保持关闭，并停止全部已有样本的后续采集；重新开启只适用于此后提交的范围，已停止样本不恢复。该桌面变更随 v0.1.3 打包发布；后台服务仍需单独部署和升级。自动同步仅覆盖已授权的范围，不调用额外模型。复用首版只采集定义、非文件输入及逐项验收，交付物仅记录所选 ID，不读取文件正文。

2026-09-11 按用户要求将对话上传触发点前移到主动记录：最近聊天、历史聊天、桌宠记录、从消息新建，以及完成后恢复记录，在参与改进开启时登记独立 `worket-recording-v1` 范围。上传该记录已有与后续的用户消息和 AI 回复；不额外读取附件正文，不包含工具事件、推理摘要或来源元数据。消息本身可能含敏感内容，不能表述为匿名化或自动脱敏。首次记录入口展示用途、90 天期限及退出入口，点击记录即提交此范围，无需先沉淀。成功开始记录后，本机持久记住已展示的说明版本，后续记录及重启不再重复提示；说明版本变化时重新展示。打开页面、取消或失败不消除首次提示，关闭参与改进时不显示上传提示。完整说明与关闭、删除入口一直保留在“Worket 服务 → 改进数据”。旧记录不会因升级、启动、打开或重新开启全局偏好而自动补登记；主动恢复记录会重新提交该记录的已有范围。

本地记录不等待服务器：离线、后台暂停或暂未升级时保留待同步数据，恢复原服务后重试；取消记录、完成、归档或关闭参与改进会停止对应后续上传并丢弃待同步正文，已发送样本仍可单独删除。上传本身不调用模型。复用创建动作仍只采集定义和验收，未主动选择记录的新工作对话不自动纳入。v0.1.3 已包含桌面端改动；改进采集新增协议仍需要兼容的后台版本才能接收。

“更多 → Worket 服务 → 改进数据”能查看同步状态、停止单个或全部样本的后续采集、删除后台样本。停止会丢弃待同步正文；已经发出的请求可能仍到达后台，彻底移除须使用删除。离线删除显示待确认，恢复原服务连接后完成。网络重试和应用重启不重复计数；服务或凭据变化时暂停上传。新范围沿用参与改进偏好，并在提交前展示实际采集内容；沉淀重试默认不加入改进采集。

当前交付通过合成数据、实际后台页面和打包桌面闭环验证。尚未导入本机真实样本，也未开展真实样本的版本效果对比；不得将自动采集可用表述为产品改进效果已验证。

2026-09-09 的 [工作定义提取执行依据](work-object-extraction-execution-plan.md) 将下一轮验证收敛为：先选一类真实重复工作，核对提取忠实度，再以新输入验证交付与用户成本。文档明确助手与用户分工，以及通过、修订和暂缓的依据；这是待执行的试点方案，不改变现有已实现能力或用户授权范围。

2026-09-21 已实现独立抽取评测 runner，并运行 E0 合成基线。runner 将模型执行与 gold/人工裁定分离，冻结代码、输入、授权、预算和 trial 账本，失败与重试不可覆盖；WorkState 本地路径完成 12 个合成案例的实际持久化与交接导出，Definition 托管路径用 `deepseek-flash` 运行 9 个案例，其中 8 个通过产品契约，冲突案例主 run 与 retry 均为 `INVALID_MODEL_OUTPUT`。这只证明评测基础和当前错误分布，尚无真实用户样本、正式语义指标或人工节省证据。详见 [抽取评测验收](acceptance/extraction-evaluation-v1.md)。

2026-09-21 用户进一步授权 VideoCreator 项目对话用于本轮评测及收费模型调用。评测导入只读取对应根对话的用户/助手可见文本，不读取独立附件正文、工具输出、隐藏推理或其他项目记录；真实材料与输出默认不入 Git。当前登记 2 份校准和 4 份试点保留记录，H1 前只允许校准 split 运行。保留记录因操作者已有项目接触而不是严格盲法，不能作为最终独立封存结论；授权不等于 gold 确认，也不等于允许长期产品采集或训练。

上述 2 份校准记录先完成本地 WorkState 产品出口基线：4/4 trial 持久化并导出，重复结果确定一致，但语义尚未经 H1。初查已看到最终 50 words 规则遗漏、旧 next step 残留、长规格截断和目标重复投影等风险。收费 Definition 调用在该阶段尚未外发；执行安全边界要求用户再明确授权将对话正文发送到 `worket.dandi.site`。

用户补充目的地授权后，收费 Definition 校准 run 已执行：4 个预注册 trial 中 2 个成功、1 个 `fetch failed`、1 个 `INVALID_MODEL_OUTPUT`。成功的同一历史两次输出在输入、约束、验收和问题数量上明显波动，并出现未确认代理建议既作为约束又作为问题的状态冲突；另一历史没有成功输出。调用预留为 8，实际失败调用数、token 与账单不可观察。该结果只构成 E0 错误分布，不能发布为真实质量提升；H1 前不运行四份试点保留记录，也不追加收费重试。

2026-09-22 已为 H1 增加本机人工确认页，并从两份校准对话的原始可见正文预填 28 条语义单位。用户在同一页完成逐条确认并签名，冻结结果为 21 条保留、7 条删除，其中 20 条进入评分分母。以后测试只需提供新的 source JSONL 与 draft JSON，无需改页面；最终 JSON 只有全部完成、证据属于同案原文且评审人签名完整时才会生成，并禁止覆盖。四份试点保留记录仍未加载到页面。

人工确认页不向评审人暴露“评分分母、关键性、目标”等内部术语，而是直接询问“本轮要求模型识别吗”“模型漏掉它会怎样”“识别后应该保存到哪里”。界面的人话标签只映射到稳定 JSON 枚举，不改变计分公式或已有评测文件。

确认顺序按评审人的阅读路径固定为：先看待确认内容，再看系统自动分类，最后判断是否正确。正确时直接进入下一条；不正确时才展开来源证据、分类依据和编辑字段。案例中的内容全部处理后自动进入下一个案例，减少确认正确条目时的无关阅读和操作。

H1 完成后，两份校准记录由真实 `deepseek-flash` 各生成一次 Working Context，并走 WorkState 持久化与 Handoff 导出。第二阶段确认页自动读取 gold、真实 trial 与模型裁定，只把 5 条 Gold 覆盖和 41 条被自动标为非 `USEFUL` 的输出交给用户确认；61 条自动标为 `USEFUL` 的输出明确保留为模型判断，不冒充人工确认。页面继续采用“内容 → 自动判断 → 是否正确”的顺序，确认正确直接进入下一条，分歧时才修改分类与对应关系。

用户已完成第二阶段 46 项风险确认。S 轨道的 5 条必需且关键要求全部覆盖，但 102 条 Working Context 输出中至少 41 条经人工确认是部分正确、重复或当前无关，确认噪音下限 40.2%；第二份长历史达到 32/44。该结果说明当前模型能抓住关键要求，但尚未把“知道过的内容”可靠压缩为当前交接所需状态。61 条正向输出仍是 `MODEL_ONLY`，因此正式精确率保持未知，不能用 59.8% 的模型估计替代全量人工结论。

### 应用内更新

采用无需 Apple 付费会员的半自动更新：打包版检查 GitHub 最新稳定 Release，用户点击下载后校验 ZIP 并在 Finder 中选中。用户保存编辑、退出应用、解压并拖动替换后重开；应用不自动安装或重启。已保存工作和设置沿用原目录。开发模式禁用检查，同一版本稍后提醒只在手动检查或下次启动出现。

### 服务连接（2026-09-18，jp-server 已部署）

v0.1.4 正式版内置 `https://worket.dandi.site`。没有既有配置时生成用户恢复码与本设备安装秘密；首次检查服务、沉淀或同步已授权上传时自动注册。模型额度、请求和上传按匿名用户归属，设备保持独立撤销。用户可复制恢复码，在另一设备恢复同一用户；恢复码原文只保存在 macOS 系统安全存储，服务器只保存哈希。

开发环境仅在显式传入 `WORKET_SERVICE_URL` 时自动接入测试服务。已有手工服务地址与令牌继续优先，不被内置地址覆盖。切换手工服务或恢复用户不会改变本地工作库；参与改进偏好保持独立。身份与数据不变量见 [托管用户身份 v1](specs/managed-user-identity-v1.md)。

新环境已验证公网 HTTPS、未鉴权 API 返回 401、管理页面公网 404、容器重启和加密主密钥持久化。当前 `/health` 返回 `configured:false`；模型配置、接入令牌和真实桌面沉淀尚未验收，不能描述为完整线上闭环。2026-09-10 至 11 日的真实 DeepSeek 与合成材料闭环仅是旧环境历史证据，见 [VPS 退役与历史验收](deployment/vps-operation.md)。

2026-09-11 贴边修正：自由拖动按可见角色边界计算，透明气泡留白不再阻挡角色到达上方与左侧；气泡在靠近顶部时向下展开。底部吸附使用物理屏幕底边，左右上沿用可用工作区。Dock 避让读取本机图标数量和尺寸估算，读取失败时预留中央 80% 屏宽，因此部分空白可能仍被保守排除。只露眼睛的三套造型见 `docs/design/pet-edge-eye-options.html`，用户已选择 B，已应用。

2026-09-11 用户确认 B「双爪偷看」：四向吸附统一采用小圆头、两只眼睛和贴边爪尖，隐藏嘴巴，露出约 19 像素；保留状态点、点击打开和拖离恢复。原生点击窗口仍为 32×68 / 68×32。

2026-09-11 双爪造型微调：露出轮廓改为宽圆角，眼睛向中间和边缘内侧收拢，保持约 19 像素露出高度及原点击窗口尺寸。

2026-09-11 贴边头部进一步调整为 48×34 椭圆，取消平底圆角，保持 19 像素露出高度、内收眼睛和贴边双爪。

2026-09-11 拖动连续性修复：角色在 Dock 左右空白区域可连续拖到物理屏幕底边，再贴边收纳，避免拖动停在 Dock 上方、松手跳到底边。中央 Dock 区域仍避让，现有椭圆双爪造型保持。

2026-09-11 贴边收纳加入约 260 毫秒吸入动作：角色沿边缘缩入，眼睛和双爪随后显露；拖出约 150 毫秒展开。沿用用户确认的扁椭圆造型。

2026-09-11 吸附期间正在记录时，绿色状态点以 2.8 秒周期轻微呼吸，眼睛和双爪保持稳定；停止记录后不再呼吸。系统减少动态效果偏好下显示静态绿点。

### 界面一致性（2026-09-21 更新）

桌面各页统一采用审阅浮窗的浅纸色、灰绿文字、绿色主操作和微笑 Logo。正文 12px、按钮 11px、辅助信息 10–11px；标准控件 28px 高、圆角 6px。列表用细线分隔，工作详情和已保存定义用分类图标与固定标签列。默认窗口 410×660，最小 360×480；横向扩展时内容区随窗口变宽。服务后台保留其现有样式。

工作列表每项只显示一次标题，复选框用于批量沉淀，无选择时不可提交。打开工作后单独显示详情，保留返回入口；交接、完成、沉淀为主要操作，复制、导出、归档和取消记录收进更多。无数据的状态栏目不展示。桌宠打开或记录工作时直接定位该项详情。

页面不放功能介绍、重复说明与内部领域术语。上传范围、用户授权及不可恢复的删除后果保留简短说明，完整采集、留存和退出规则可展开查看。此调整不改变采集范围、退出偏好和原始对话的保护边界。详见 [界面验收](qa/interface-consistency.md)。

2026-09-11 用户复核：保留简洁布局，恢复暖黄纸感配色并缩小面板。详情移除黄色说明横幅，交接状态并入辅助信息；已在记录入口说明的上传范围不在详情重复展示，首次入口说明和服务设置保留。错误与操作反馈用无底色的短文本显示。

资料与产物中的本地文件仅显示可点击的文件名，悬停查看完整路径，点击由系统默认应用打开；文件不存在或已移动时显示简短错误。文件条目不再使用大卡片。

2026-09-11 浏览体验：查看详情只展示已保存的资料信息，不为显示文件名读取附件；原文件访问仍发生在打开、主动刷新、交接等实际使用环节，新增资料挂接仍记录文件元数据。右侧及弹窗滚动条统一为细窄暖灰色，轨道透明。

2026-09-11 记录进度修复：自动轮询、Hook 与手动刷新统一更新来源档案和工作状态。状态更新进度在本地保存，重启后继续处理已记录但尚未提炼的消息；提炼失败不丢失原记录。最近回复提供执行者回复的前三段预览（2026-09-21 起按需展开），帮助查看最新执行情况；该回复不等于用户验收通过。

2026-09-11 已发布：v0.1.2 汇总界面一致性、资料链接、细滚动条、桌宠贴边交互，以及来源已记录但详情状态未更新的修复。版本说明见 [v0.1.2](releases/v0.1.2.md)，安装包继续采用 macOS arm64 ZIP 与 SHA256，沿用现有数据目录和手动替换更新方式。

## 中文文档式候选审阅（2026-09-18）

新生成的沉淀候选默认使用简体中文；专名、代码、变量和原始引用保留原样，英文交付要求仍保留其语言约束。审阅交互已于 2026-09-21 更新为下述紧凑浮窗；原有来源核验、修订及发布校验继续生效。既有候选不自动翻译。

## 后台沉淀与桌宠反馈（2026-09-18）

开始沉淀后关闭范围弹窗，面板显示后台进度入口，用户可继续浏览或关闭面板。进度来自实际分批处理与汇总阶段，不估算完成百分比。桌宠处理中显示状态点与纸条动效；候选完成后轻跳并显示审阅入口；失败时显示处理入口与中文原因。完成不强制弹窗，点击桌宠直达对应任务；查看后的提醒持久消除，仍可从已沉淀的任务列表找回。关闭面板继续运行；退出整个应用不能保证服务结果在过期前被收取。

来源误标不会当成用户原话保存：实际引用为 Agent 或工具记录时降为系统推断，并生成必须处理的确认问题。不存在的引用或无法匹配的摘录仍拒绝。

## 紧凑浮窗审阅（2026-09-21）

候选在默认 410px 的现有桌面面板内按目的、输入、交付、约束、验收、方法、资料紧凑排列，分类配小图标。右边缘可拖动调宽，左边界与分类列保持固定；超出当前屏幕可用宽度时停止扩展。长内容在正文区域滚动，操作栏固定。

待定事项尽量放在对应条目旁：保留、参考、按次交付、不纳入或采用修改都会保存实际决定；按次交付会新增必填的是或否输入并修改交付条件。无法唯一关联的事项单独显示，不猜测归属；系统原话与推断依据按需展开。未处理事项继续阻止确认保存。

“编辑全部”进入第二层：可改工作名称和各条文字、增删条目、切换输入必填和方法义务。输入类型、可选值及默认值在条目设置中编辑，固定资料沿用显式选取。暂存保留真实修改和决策；重新决定可撤销已存确认，修改关联要求会重新要求确认。关闭未暂存内容会提示是否放弃。

此轮为源码与本地桌面交付，不代表已经发布新版本；验收见 [紧凑审阅验收](acceptance/compact-definition-review.md)。

2026-09-21 全桌面统一：历史、服务设置、沉淀准备与任务、已保存定义、复用及验收均在同一浮窗内展示；长页面滚动正文，主要操作固定在底部。小型交接、拆分和取消记录确认保留轻量弹层。空资料与空案例不占位，固定资料显示用途与文件名，完整路径悬停可查；最近回复和执行片段按需展开，刷新时保留展开状态。授权范围、退出入口和删除后果仍保留。
