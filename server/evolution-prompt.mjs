export const EVOLUTION_PROMPT = `
INCREMENTAL CONTRACT UPDATE. The baseline is a confirmed contract supplied by the user, not a new user message. Do not rewrite it. Preserve untouched rules, inputs, materials and purpose by returning NO change for them. Compare every supported requirement in the new evidence against the effective baseline items (target addresses are section.key).
Return the usual top-level schema/compatibility/issues/groups/requirements/coverage/versions, but content MUST be null. Add evolution:{baseHash:"the exact baseline.contentHash",changes:[{kind:"ADD"|"DUPLICATE"|"REPLACES"|"SUPPLEMENTS"|"CONFLICT",section:"purpose"|"inputs"|"deliverables"|"constraints"|"acceptanceCriteria"|"methods"|"materialRoles",item:Item,target?:"baseline section.key"}]}.
Each item carries evidence from NEW supplied events only, with the same Item schema and document rules. Baseline text is not new source evidence. Do not create entries just to repeat baseline items that the new messages do not mention. Repeated new statements of an existing obligation are DUPLICATE and only add evidence. Unique new requirements are ADD. New enduring corrections use REPLACES; new distinct detail uses SUPPLEMENTS with only the added detail. Ambiguous contradictions use CONFLICT and a blocking issue. All non-ADD targets MUST exist in the baseline, never in another change. ADD has no target. Do not put relation in item.rule: change.kind/target carry that relationship. All rule items must specify scope/status/condition. DUPLICATE must be ACTIVE and share the target scope/condition; REPLACES must also share scope/condition. A one-time exception is ADD with INSTANCE scope, never REPLACES a reusable rule. Unaccepted suggestions stay PROPOSED; uncertainty stays UNCERTAIN with a blocking issue. Preserve negation, thresholds and conditions. Separate independent obligations into atomic changes.
Use stable unique ASCII item keys within each section. For inputs/materialRoles/purpose, DUPLICATE or REPLACES must use the target's original key and same section; inputs/materialRoles may also ADD a new unique key. A changed input type, required flag, choices/default, or material required flag is REPLACES, not DUPLICATE. Purpose cannot ADD. Preserve input placeholders using existing baseline input keys or new ADD input keys. A work.input_provided event supplies a value for this instance, not a new statement or reaffirmation of the input role. Do not emit an input DUPLICATE just because a new value was supplied. Inputs include valueType/required and necessary choices; methods include obligation; materialRoles include required. A method's obligation cannot change through DUPLICATE. A correction need not restate unchanged purpose, deliverables, inputs or methods. Empty changes is valid if no useful requirement is present. For UNRELATED return content:null, groups explaining the separation, and no evolution.
Issues for changes use field "section.itemKey". Never claim user confirmation. All changes remain a draft until reviewed. Coverage covers only NEW event keys, not the baseline. No deletion by omission. No tool use. No generic rules unsupported by new evidence.`;
